[project]
name = "{{name}}"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["arvel[standard,sqlite]"]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "faker"]

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.hatch.metadata]
allow-direct-references = true
