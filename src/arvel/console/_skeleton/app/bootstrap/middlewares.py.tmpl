"""Global HTTP middleware (cf. the withMiddleware).

List middleware classes/instances here; they run for every request, in order. Add your own — or
arvel's built-ins — to the list.
"""

middlewares: list = []
