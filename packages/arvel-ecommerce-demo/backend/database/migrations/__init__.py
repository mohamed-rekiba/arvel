# Migration sequence note: 000004 is intentionally absent.
# It was used during development for a users table migration that was merged
# into the RBAC tables migration (000000) before the first public commit.
# Do not create a new 000004 migration — use the next available number instead.
