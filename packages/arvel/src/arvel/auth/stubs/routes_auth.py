"""Auth route stubs — published to routes/auth.py by `arvel auth:install`.

The routes are already registered automatically by AuthServiceProvider when
``config/auth.py`` has ``routes.enabled = True``.  Import this file only when
you need to add custom auth-related routes.
"""

from __future__ import annotations
# Add custom auth routes here.
