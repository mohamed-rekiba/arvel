"""Command name -> short help, used to render ``arvel --help`` without
importing command implementations. Generated by scripts/gen_command_meta.py;
a drift-guard test keeps it in sync. Do not edit by hand."""

from __future__ import annotations

COMMAND_HELP: dict[str, str] = {
    "about": "Display arvel framework information",
    "auth:clear-resets": "Delete expired password_resets rows",
    "cache:clear": "Flush all items from the cache",
    "cache:forget": "Remove a specific key from the cache",
    "channel:list": "List registered broadcasting channels",
    "config:cache": "Serialize the loaded config registry to bootstrap/cache/config.json.",
    "config:clear": "Delete the cached config file so the next boot reads config/*.py.",
    "config:publish": "Publish package config files into the app",
    "config:show": "Print a resolved dotted config value",
    "db:seed": "Run database seeders",
    "db:show": "Print database connection and table summary",
    "db:table": "Print columns and indexes for a table",
    "down": "Put the application into maintenance mode",
    "event:list": "List registered events and their listeners",
    "key:generate": "Generate a new APP_KEY (base64) and write it to .env",
    "key:rotate": "Re-encrypt all encrypted columns with a new key (not yet implemented)",
    "make:cast": "Generate a custom column cast (SQLAlchemy TypeDecorator)",
    "make:channel": "Generate a broadcast channel authorization callback",
    "make:command": "Generate an Arvel console command",
    "make:controller": "Generate an HTTP controller (subclass of arvel.Controller)",
    "make:event": "Generate a Pydantic event class",
    "make:factory": "Generate a model factory (arvel.database.Factory)",
    "make:job": "Generate a queued Job (Pydantic + handle())",
    "make:listener": "Generate an event listener (Listener[E] + async handle)",
    "make:mail": "Generate a Mailable (envelope() + content())",
    "make:middleware": "Generate HTTP middleware (handle(request, call_next))",
    "make:migration": "Create a new migration file under database/migrations/",
    "make:model": "Generate an ORM model (arvel.database.Model)",
    "make:notification": "Generate a Notification (via + to_mail/to_database/to_broadcast)",
    "make:observer": "Generate a model lifecycle Observer",
    "make:policy": "Generate an authorization Policy (async ability methods)",
    "make:provider": "Generate a ServiceProvider (register/boot/commands/shutdown)",
    "make:request": "Generate a typed FormRequest (Pydantic payload + authorize)",
    "make:resource": "Generate a JsonResource transformer (arvel.http.JsonResource)",
    "make:schema": "Generate Pydantic Create/Update/Read schemas from a model",
    "make:seeder": "Generate a database seeder (arvel.database.Seeder)",
    "make:service": "Generate an application service class",
    "make:test": "Generate a feature test (pytest + TestClient)",
    "make:view": "Generate a Jinja view template (extends layouts/base.html)",
    "migrate": "Run pending database migrations",
    "migrate:fresh": "Drop all tables and re-run every migration",
    "migrate:refresh": "Roll back every migration, then re-run them",
    "migrate:reset": "Roll back every applied migration in reverse order",
    "migrate:rollback": "Roll back the last batch of migrations",
    "migrate:status": "Show migration status",
    "model:prune": "Delete stale rows for all Prunable models.",
    "model:show": "Print model metadata (table, attributes, visible/hidden)",
    "new": "Scaffold a new Arvel project from the packaged skeleton",
    "openapi:export": "Export the OpenAPI spec to a file (YAML or JSON).",
    "openapi:validate": "Validate the OpenAPI spec against the OpenAPI 3.x schema.",
    "optimize": "Pre-compile config and view caches for production.",
    "optimize:clear": "Remove all production caches (config, views).",
    "queue:clear": "Remove all pending jobs from a queue",
    "queue:prune-failed": "Delete failed jobs older than --hours",
    "queue:restart": "Signal running queue workers to restart gracefully",
    "reverb:start": "Start the Reverb WebSocket server.",
    "route:list": "List all registered routes (method, URI, name, action, middleware)",
    "schedule:continue": "Resume task dispatching after schedule:pause.",
    "schedule:interrupt": "Signal the running scheduler to exit at the next tick boundary.",
    "schedule:list": "List registered scheduled tasks.",
    "schedule:pause": "Stop the scheduler from dispatching tasks each tick.",
    "schedule:run": "Alias for `schedule:work --once` \u2014 run the scheduler once and exit.",
    "schedule:work": "Run the scheduler dispatch loop (foreground).",
    "serve": "Run the project ASGI app under uvicorn (defaults to public.asgi:asgi)",
    "shell": "Launch an interactive REPL with the Arvel app bootstrapped",
    "storage:link": "Create a symbolic link from public/storage to storage/app/public",
    "storage:unlink": "Remove the public/storage symlink (idempotent)",
    "test": "Run pytest against the application",
    "tinker": "Alias for `shell` \u2014 interactive Python REPL with Arvel bootstrapped",
    "up": "Bring the application out of maintenance mode",
    "vendor:publish": "Publish package files (migrations, config, assets) into the app",
    "view:cache": "Pre-compile all Jinja templates into bootstrap/views/.",
    "view:clear": "Clear the cached Jinja environment and bytecode cache",
}
