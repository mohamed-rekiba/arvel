"""Stub migrations shipped with arvel.session.

Published into the consumer app via ``arvel vendor:publish --tag=arvel-session``.
Discovered by the migrator by filesystem path, not by Python import.
"""
