# Authentication Scaffolding

Arvel's first-party auth ships nine endpoints out of the box. Run the install command to wire them:

```bash
arvel auth:install
```

See [Authentication](authentication.md) for the full reference — login, registration, email verification, password reset, token refresh, and logout are all included.
