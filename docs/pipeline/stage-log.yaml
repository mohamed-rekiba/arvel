schema_version: 1
events:
  - work_item_id: "WI-arvel-001"
    event_id: "EV-001"
    timestamp: "2026-05-30T14:42:00+03:00"
    from_stage: null
    to_stage: "planning-sa"
    role: "solution-architect"
    result: "pass"
    notes: "ADR-123 authored (nested groups, unless/tap, EXISTS). SAD/OpenAPI N/A — no system/HTTP surface."
    handoff_id: null

  - work_item_id: "WI-arvel-001"
    event_id: "EV-002"
    timestamp: "2026-05-30T14:46:00+03:00"
    from_stage: "planning-sa"
    to_stage: "qa-pre"
    role: "qa-engineer"
    result: "pass"
    notes: "10 failing tests authored in test_qb_conditionals_existence.py (Red confirmed: 8 fail, 2 behavioral pass)."
    handoff_id: null

  - work_item_id: "WI-arvel-001"
    event_id: "EV-003"
    timestamp: "2026-05-30T14:50:00+03:00"
    from_stage: "qa-pre"
    to_stage: "execution"
    role: "developer"
    result: "pass"
    notes: >
      Implemented where()/or_where() callable group support, unless(), tap(),
      EXISTS-based exists()/doesnt_exist(), and query_mixin class forwarders.
      All 10 tests green.
    handoff_id: null

  - work_item_id: "WI-arvel-001"
    event_id: "EV-004"
    timestamp: "2026-05-30T14:55:00+03:00"
    from_stage: "execution"
    to_stage: "qa-post"
    role: "qa-engineer"
    result: "pass"
    notes: >
      Regression caught & fixed: exists() with_only_columns(literal(1)) dropped
      FROM clause (always-true). Rebuilt EXISTS from scoped select. Gates:
      708 db tests pass, mypy clean (870 files), pyright 0/0/0, ruff lint+format
      clean, bandit 0, pip-audit clean, gitleaks clean.
    handoff_id: null

  - work_item_id: "WI-arvel-001"
    event_id: "EV-005"
    timestamp: "2026-05-30T14:58:00+03:00"
    from_stage: "qa-post"
    to_stage: "operations"
    role: "devops"
    result: "pass"
    notes: >
      Security-review (4b) skipped: L2/Tier 2, no auth/PII surface. Release
      skipped: library code increment, no deployable artifact (gates verified
      in qa-post). ROADMAP Sprint A updated (S2/S4/S7 marked DONE).
    handoff_id: null

  - work_item_id: "WI-arvel-002"
    event_id: "EV-006"
    timestamp: "2026-05-30T15:01:00+03:00"
    from_stage: null
    to_stage: "planning-sa"
    role: "solution-architect"
    result: "pass"
    notes: "ADR-124: ContextVar-based re-entrant event suppression + quiet wrappers."
    handoff_id: null

  - work_item_id: "WI-arvel-002"
    event_id: "EV-007"
    timestamp: "2026-05-30T15:03:00+03:00"
    from_stage: "planning-sa"
    to_stage: "qa-pre"
    role: "qa-engineer"
    result: "pass"
    notes: "9 failing tests in test_without_events_quiet.py (Red confirmed)."
    handoff_id: null

  - work_item_id: "WI-arvel-002"
    event_id: "EV-008"
    timestamp: "2026-05-30T15:05:00+03:00"
    from_stage: "qa-pre"
    to_stage: "execution"
    role: "developer"
    result: "pass"
    notes: >
      events.py: _suppress_events ContextVar, without_events() async CM, early-return
      in fire_async/fire_cancellable/fire_after_commit. model.py: without_events()
      classmethod + save/delete/force_delete/restore/update _quietly. 9 tests green.
    handoff_id: null

  - work_item_id: "WI-arvel-002"
    event_id: "EV-009"
    timestamp: "2026-05-30T15:07:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      Fixed 2 pyright strict findings (asynccontextmanager AsyncGenerator return
      type, unused import). Gates: 717 db tests, mypy clean (871 files), pyright
      0/0/0, ruff clean, bandit 0, pip-audit clean, gitleaks clean. 4b + release
      skipped (L2/Tier2, library increment).
    handoff_id: null

  - work_item_id: "WI-arvel-003"
    event_id: "EV-010"
    timestamp: "2026-05-30T15:08:00+03:00"
    from_stage: null
    to_stage: "planning-sa"
    role: "solution-architect"
    result: "pass"
    notes: >
      ADR-125: write-only hashed cast over __casts__ dispatch (_READ_SKIP_CASTS),
      force_fill, scoped unguarded() context (no global toggle, security rationale).
    handoff_id: null

  - work_item_id: "WI-arvel-003"
    event_id: "EV-011"
    timestamp: "2026-05-30T15:09:00+03:00"
    from_stage: "planning-sa"
    to_stage: "qa-pre"
    role: "qa-engineer"
    result: "pass"
    notes: "6 tests in test_hashed_cast_unguard.py (Red: validation rejects 'hashed')."
    handoff_id: null

  - work_item_id: "WI-arvel-003"
    event_id: "EV-012"
    timestamp: "2026-05-30T15:11:00+03:00"
    from_stage: "qa-pre"
    to_stage: "execution"
    role: "developer"
    result: "pass"
    notes: >
      model.py: _hashed_cast + dispatch entry, _READ_SKIP_CASTS, __getattribute__
      read-skip, _mass_assignment_unguarded ContextVar, force_fill, unguarded().
      6 tests green; fixed @contextmanager Generator return-type deprecation.
    handoff_id: null

  - work_item_id: "WI-arvel-003"
    event_id: "EV-013"
    timestamp: "2026-05-30T15:12:00+03:00"
    from_stage: "execution"
    to_stage: "security-review"
    role: "qa-engineer"
    result: "pass"
    notes: >
      QA-Post gates: 723 db tests, mypy clean (872 files), pyright 0/0/0, ruff
      clean, bandit 0, pip-audit clean, gitleaks clean.
    handoff_id: null

  - work_item_id: "WI-arvel-003"
    event_id: "EV-014"
    timestamp: "2026-05-30T15:13:00+03:00"
    from_stage: "security-review"
    to_stage: "operations"
    role: "code-reviewer"
    result: "pass"
    notes: >
      Stage 4b (Tier 3, credential hashing). OWASP A04/A05: argon2id via Hash
      facade (no weak hash), idempotent on PHC prefixes, read-path skipped.
      force_fill/unguarded explicit-only, bounded (task-local ContextVar, no global
      toggle), no plaintext logging. PASS — no critical/high. Closes Parity Sprint A.
    handoff_id: null

  - work_item_id: "WI-arvel-004"
    event_id: "EV-015"
    timestamp: "2026-05-30T15:45:00+03:00"
    from_stage: null
    to_stage: "planning-sa"
    role: "solution-architect"
    result: "pass"
    notes: >
      ADR-126: closure-form DB.transactional retry (separate from CM transaction()),
      retryable predicate scoped to deadlock/serialization. Imperative txn control deferred.
    handoff_id: null

  - work_item_id: "WI-arvel-004"
    event_id: "EV-016"
    timestamp: "2026-05-30T15:48:00+03:00"
    from_stage: "planning-sa"
    to_stage: "qa-pre"
    role: "qa-engineer"
    result: "pass"
    notes: "6 tests in test_transaction_retry.py (Red: import of helper fails)."
    handoff_id: null

  - work_item_id: "WI-arvel-004"
    event_id: "EV-017"
    timestamp: "2026-05-30T15:52:00+03:00"
    from_stage: "qa-pre"
    to_stage: "execution"
    role: "developer"
    result: "pass"
    notes: >
      db.py: is_retryable_db_error predicate + DB.transactional(callback, attempts).
      6 tests green. Promoted helper to public (renamed off _ prefix) per quality-gate
      rule when test needed direct access.
    handoff_id: null

  - work_item_id: "WI-arvel-004"
    event_id: "EV-018"
    timestamp: "2026-05-30T15:56:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      Gates: 729 db tests, mypy clean (873 files), pyright 0/0/0, ruff clean, bandit 0,
      pip-audit clean, gitleaks clean. 4b + release skipped (L2/Tier2, library increment).
      S12 marked PARTIAL in ROADMAP.
    handoff_id: null

  - work_item_id: "WI-arvel-005"
    event_id: "EV-019"
    timestamp: "2026-05-30T18:05:00+03:00"
    from_stage: "qa-post"
    to_stage: "planning-sa"
    role: "solution-architect"
    result: "pass"
    notes: >
      006-S1 attribute custom cast protocol. ADR-127: CastsAttributes ABC (get/set/serialize),
      resolvers cached at __init_subclass__ to keep __getattribute__ hot path cheap,
      parameterized "decimal:N" specs. Registry-named cast form deferred (pass class directly).
    handoff_id: null

  - work_item_id: "WI-arvel-005"
    event_id: "EV-020"
    timestamp: "2026-05-30T18:08:00+03:00"
    from_stage: "planning-sa"
    to_stage: "execution"
    role: "developer"
    result: "pass"
    notes: >
      CastsAttributes (non-generic ABC) in attributes.py; _ResolvedCast resolver pipeline +
      _resolve_cast_spec in model.py; __getattribute__/__setattr__/to_dict route through
      resolvers; built-in read/write-skip semantics (hashed, json) preserved. 8 tests green.
    handoff_id: null

  - work_item_id: "WI-arvel-005"
    event_id: "EV-021"
    timestamp: "2026-05-30T18:11:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      Gates: arvel 3448 tests, arvel-permission 75, ecommerce-demo 407 (own sessions) —
      hot-path rewrite, no regressions. mypy clean (874 files), pyright 0/0/0, ruff + format
      clean, security clean. 4b + release skipped (L2/Tier2). 006-S1 marked DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-006"
    event_id: "EV-022"
    timestamp: "2026-05-30T19:45:00+03:00"
    from_stage: "qa-post"
    to_stage: "qa-pre"
    role: "qa-engineer"
    result: "pass"
    notes: >
      006-S4 cast-aware dirty tracking. TDD (proper red first): 7 tests in
      test_cast_aware_dirty.py — boolean/int + decimal-string + re-serialized-JSON
      equivalence, get_raw_original, get_original casts, original_is_equivalent. Red: JSON
      reserialization false-positive + missing methods.
    handoff_id: null

  - work_item_id: "WI-arvel-006"
    event_id: "EV-023"
    timestamp: "2026-05-30T19:55:00+03:00"
    from_stage: "qa-pre"
    to_stage: "execution"
    role: "developer"
    result: "pass"
    notes: >
      model.py: original_is_equivalent (read-cast comparison, Eloquent parity),
      get_raw_original (pre-cast) + get_original (cast) split, is_dirty/get_dirty filter via
      equivalence. NO_VALUE sentinel guarded in original_is_equivalent + _read_cast (pending
      instances during create() snapshot). 15 dirty tests green.
    handoff_id: null

  - work_item_id: "WI-arvel-006"
    event_id: "EV-024"
    timestamp: "2026-05-30T20:06:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      Gates: arvel 744 db tests, permission 75, ecommerce 407 (own sessions). mypy clean
      (875 files), pyright 0/0/0, ruff + format clean, security clean. 4b + release skipped
      (L2/Tier2). 006-S4 marked DONE. ADR-128.
    handoff_id: null

  - work_item_id: "WI-arvel-007"
    event_id: "EV-025"
    timestamp: "2026-05-30T20:45:00+03:00"
    from_stage: "planning"
    to_stage: "qa-pre"
    role: "solution-architect"
    result: "pass"
    notes: >
      005-S10 streaming + chunking completeness. ADR-129: stream() server-side cursor
      (stream_scalars + yield_per) distinct from lazy(); directional keyset via descending=
      on chunk_by_id + new lazy_by_id; callbacks stop on False; offset chunk auto-orders by PK
      (Eloquent parity). TDD: 7 tests in test_streaming_completeness.py, red first (5 fail).
    handoff_id: null

  - work_item_id: "WI-arvel-007"
    event_id: "EV-026"
    timestamp: "2026-05-30T20:55:00+03:00"
    from_stage: "qa-pre"
    to_stage: "execution"
    role: "developer"
    result: "pass"
    notes: >
      query.py: _ordered_for_offset (PK fallback), _keyset_batches shared generator,
      directional chunk_by_id/lazy_by_id, stream() via stream_scalars, False-callback stop on
      chunk/chunk_by_id/each. query_mixin forwarders updated + lazy_by_id/stream added.
      queries.md documented. 12 streaming tests green.
    handoff_id: null

  - work_item_id: "WI-arvel-007"
    event_id: "EV-027"
    timestamp: "2026-05-30T21:02:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      Gates: arvel 751 db tests (744 + 7 new), mypy clean, pyright 0/0/0, ruff clean, bandit +
      gitleaks clean. 4b + release skipped (L2/Tier2). 005-S10 marked DONE. ADR-129.
    handoff_id: null

  - work_item_id: "WI-arvel-008"
    event_id: "EV-028"
    timestamp: "2026-05-30T21:15:00+03:00"
    from_stage: "planning"
    to_stage: "execution"
    role: "solution-architect"
    result: "pass"
    notes: >
      005-S13 + WHERE predicate engine. ADR-130: QueryBuilder accumulates WHERE in
      _where_predicate (ColumnElement|None) applied in apply_global_scopes/statement, enabling
      real or_where/or_where_* OR-onto-chain with explicit grouping ((a OR b) AND c). Routed
      all where_* through _and, or_where_* through _or. Clause bundle: order_by_desc, reorder,
      in_random_order, group_by_raw, operator-form having + having_null/between, pluck(value,
      key)->dict, count(column), sum()->0.
    handoff_id: null

  - work_item_id: "WI-arvel-008"
    event_id: "EV-029"
    timestamp: "2026-05-30T21:30:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      Gates: arvel full fast suite 3483 pass (incl 11 new test_qb_clause_polish), permission 75,
      ecommerce 407, mypy clean, pyright 0/0/0, ruff clean, bandit + gitleaks clean. union/
      union_all fixed to clear baked-in predicate; sum-empty test updated to 0 (Laravel parity).
      4b + release skipped (L2/Tier2). 005-S13 marked DONE. ADR-130.
    handoff_id: null

  - work_item_id: "WI-arvel-009"
    event_id: "EV-030"
    timestamp: "2026-05-30T21:45:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      005 S1 + S5 + S6 bundle. ADR-131. Date/time helpers (where_year/month/day/date/time +
      or_) via extract(); LIKE (where_like/not_like + or_, case_sensitive flag), multi-column
      (where_all/where_none/or_where_any); joins (cross_join, fluent join_on on/or_on,
      right_join via target-LEFT-JOIN-model rewrite). Gates: arvel 780 db tests (+18); mypy/
      pyright/ruff/bandit/gitleaks clean. 4b + release skipped (L2/Tier2). S1/S5/S6 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-010"
    event_id: "EV-031"
    timestamp: "2026-05-30T22:05:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      005 S12 imperative-control follow-up (retry shipped earlier). ADR-126-03 updated.
      DB.begin_transaction/commit/rollback via context-local _TxnFrame stack; outermost owns
      session + after-commit queue, nested opens begin_nested() savepoint; composes with
      DB.transaction() (savepoint when a session is already active). Gates: arvel 788 db
      tests (+8); mypy/pyright/ruff/bandit/gitleaks clean. 4b + release skipped (L2/Tier2).
      005-S12 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-011"
    event_id: "EV-032"
    timestamp: "2026-05-30T22:35:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      005 S8 write-path completeness. ADR-132. insert_or_ignore (ON CONFLICT DO NOTHING /
      INSERT IGNORE), single-statement upsert returning affected count (manual fallback when
      unique_by lacks a PK/UNIQUE), truncate (TRUNCATE on PG/MySQL via quoted preparer, DELETE
      on SQLite; hard wipe), insert_using (INSERT … SELECT honoring source global scopes),
      increment_each/decrement_each (multi-col single UPDATE). Gates: arvel 794 db tests (+6);
      mypy/pyright/ruff/bandit (0 issues)/gitleaks clean. 4b + release skipped (L2/Tier2).
      005-S8 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-012"
    event_id: "EV-033"
    timestamp: "2026-05-30T23:05:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      005 S3 subquery FROM/JOIN/SELECT. ADR-133. from_sub (derived table → dict rows, clears
      inherited WHERE), join_sub/left_join_sub (closure receives the aliased subquery),
      select_sub (correlated scalar subquery as labeled column), add_select (append model cols
      or SQLAlchemy expressions). Appended columns attach onto the model via the existing
      __with_agg__ path. Gates: arvel 799 db tests (+5); mypy/pyright/ruff/bandit/gitleaks
      clean. 4b + release skipped (L2/Tier2). 005-S3 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-013"
    event_id: "EV-034"
    timestamp: "2026-05-30T23:35:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      005 S11 debugging + query-log parity. ADR-134. DB.enable_query_log/get_query_log/
      flush_query_log/disable_query_log capture {sql, bindings, time_ms} via engine cursor
      events; DB.pretend(cb) runs in a rolled-back transaction and returns the log; QueryBuilder
      to_raw_sql/get_bindings/explain (EXPLAIN QUERY PLAN on SQLite). Gates: arvel 805 db tests
      (+6); mypy/pyright/ruff/bandit (0)/gitleaks clean. 4b + release skipped (L2/Tier2).
      005-S11 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-014"
    event_id: "EV-035"
    timestamp: "2026-05-30T23:55:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      005 S9 pagination HTTP + JSON parity (last 005 story). ADR-135. PaginationRequest
      contextvar set by ObservabilityMiddleware; paginate/simple_paginate resolve page via
      page_name; Paginator.to_response() flat Laravel envelope with windowed links array
      (on_each_side) + Previous/Next bookends; appends/with_query_string/fragment chainable;
      cursor_paginate bidirectional (next_cursor + prev_cursor, walks both ways, token carries
      direction flag) + flat to_response(). Gates: arvel full suite 3606 passed; demo backend
      407 passed; mypy/pyright/ruff/bandit (0)/gitleaks clean. EPIC 005 COMPLETE. 005-S9 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-017"
    event_id: "EV-036"
    timestamp: "2026-05-30T23:05:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      006 S6 enum + extended built-in casts (first 006 story this wave). ADR-136. __casts__
      accepts an Enum subclass (read->member, write/serialize->backing value); object cast
      (JSON->SimpleNamespace, serialize->dict) and collection cast (JSON->Collection,
      serialize->list) are read-path-only on write like array; datetime:FORMAT reads via
      strptime with ISO fallback and serializes via strftime. decimal:n shipped in WI-005.
      to_dict serialize skips None (symmetric with read path). Gates: arvel database suite
      830 passed; mypy/pyright/ruff clean on model.py. 4b + release skipped (L2/Tier2,
      no auth/PII). 006-S6 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-018"
    event_id: "EV-037"
    timestamp: "2026-05-30T23:25:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      006 S3 declarative encrypted casts. ADR-137. New arvel.encryption.Encrypter (AES-256-GCM,
      32-byte key HKDF-derived from APP_KEY, wire v2 distinct from EncryptedType v1) + Crypt
      facade (per-APP_KEY cache, set_encrypter for tests, MissingAppKeyError). __casts__
      encrypted / encrypted:json|array|object|collection: write encrypts (JSON for non-string),
      read decrypts + decode (object->SimpleNamespace, collection->Collection), serialize emits
      dict/list. to_dict exposes decrypted value (Eloquent toArray; pair with __hidden__).
      Random IV per write. Tier3/confidential (PII) — security embedded in QA per A04 (key from
      APP_KEY env, never code); no auth-flow change so no dedicated 4b. Gates: arvel database
      846 passed (+16); session 65; mypy/pyright/ruff/bandit clean. 006-S3 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-019"
    event_id: "EV-038"
    timestamp: "2026-05-30T23:40:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      006 S5 unified Attribute descriptor. ADR-138. Data descriptor (get/set/__set_name__) as a
      class attr — fires via the descriptor protocol since Model delegates getattr/setattr to
      object; no metaclass scan. Attribute.make(get=, set=); set returns column->value Mapping
      (routed through normal setattr/casts), non-mapping raises TypeError; get-only=read-only,
      set-only=write-only. should_cache() memoizes per instance, invalidated on write through
      the attr. Exported from arvel.database. Gates: arvel database 855 passed (+9);
      mypy/pyright/ruff clean. 4b + release skipped (L2/Tier2, no auth/PII). 006-S5 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-020"
    event_id: "EV-039"
    timestamp: "2026-05-30T23:05:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      006 S9 static event registration + custom event objects. ADR-139. Model.on(event, cb) wraps
      a callable in a one-event _CallbackObserver on the shared observer list (cancellable + async
      preserved). __observed_by__ auto-registers in __init_subclass__ from cls.__dict__.
      __dispatches_events__ maps lifecycle->ModelEvent dispatched on the Event facade after the
      observer loop; no dispatcher bound => skipped. ModelEvent is a top-level frozen Pydantic Event
      (arbitrary_types_allowed) defined directly so subclassing type-checks. Also resolved
      pre-existing strict-pyright findings the gate surfaced (qb test column 'first'->'given' shadow
      fix, cast() narrowing, unused-symbol cleanup). Gates: arvel database+events 892 passed;
      mypy/pyright/ruff/bandit clean. 4b + release skipped (L2/Tier2, no auth/PII). 006-S9 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-021"
    event_id: "EV-040"
    timestamp: "2026-05-30T23:20:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      006 S12 timestamp controls. ADR-140. __timestamps__ toggle + CREATED_AT/UPDATED_AT str
      ClassVars on Model. Hook attachment relocated from Timestamps mixin to Model.__init_subclass__
      — attaches before_insert/before_update only when timestamps enabled AND the named attr exists
      (plain models free, custom-column models work without the mixin). without_timestamps() async CM
      via _suppress_timestamps ContextVar for imports/backfills. touch(attribute=None) saves through
      the event path; touch_quietly suppresses events; replicate drops constant-named ts cols. Gates:
      arvel database+events 901 passed (+9); mypy/pyright/ruff/bandit clean. 4b + release skipped
      (L2/Tier2, no auth/PII). 006-S12 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-022"
    event_id: "EV-041"
    timestamp: "2026-05-30T23:40:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      006 S10 soft-delete upsert + bulk restore. ADR-141. restore_or_create/create_or_restore search
      with_trashed by attributes, restore if trashed() else return, create merged dicts if none — no
      duplicate of a soft-deleted row. Bulk QueryBuilder.restore() = single UPDATE deleted_at=NULL
      over WHERE (inverse of delete() soft branch, touches updated_at); pair with only_trashed since
      default scope hides trashed; AttributeError without SoftDeletes; bulk bypasses per-row events.
      trashed() instance helper; force_destroy(*ids) varargs-or-iterable -> where_in(pk).force_delete
      (trashed included, single-PK). Gates: arvel database+events 909 passed (+8);
      mypy/pyright/ruff/bandit clean. 4b + release skipped (L2/Tier2, no auth/PII). 006-S10 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-023"
    event_id: "EV-042"
    timestamp: "2026-05-30T23:55:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      006 S11 distinct soft/hard-delete + replicate events. ADR-142. New events added to events.py
      registries: trashed/force_deleted/replicating (_ASYNC_EVENTS), force_deleting (_CANCELLABLE).
      Soft delete() fires trashed then deleted (deleted still fires for both kinds). force_delete()
      fires force_deleting->deleting->deleted->force_deleted, never trashed (the distinguishing
      signal); both before-hooks cancellable. replicate() fires replicating on the CLONE before
      return. Bulk QB deletes stay event-free (set-based, no loaded rows — ADR-142-04, Eloquent
      parity; AC's bulk-trashed mention intentionally not implemented). Observer docstring updated.
      Gates: arvel database+events 914 passed (+5); mypy/pyright/ruff/bandit clean. 4b + release
      skipped (L2/Tier2, no auth/PII). 006-S11 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-024"
    event_id: "EV-043"
    timestamp: "2026-05-31T00:10:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      006 S13 factory enhancements. ADR-143. has_attached(relation, factory, count, pivot) creates
      related rows post-flush and links via BelongsToMany.attach(pk, **pivot) (no session.expire —
      accessor self-invalidates). trashed() state sets the soft-delete col post-construction
      (init=False col), AttributeError without SoftDeletes. after_making/after_creating now get a
      shared lazily-imported faker.Faker (None if not installed; dev-only dep). create_quietly()
      wraps create() in without_events(). connection(name) routes the whole build through
      DB.session_maker_for(name) (new public maker lookup) bound as active session + commit; added
      DB.forget_named(). create() refactored to _create_in_active_session(). Pyright/mypy tension on
      list narrowing solved with _as_list() building into a pre-typed list (no cast/ignore). Gates:
      arvel database+events 921 passed (+7); mypy/pyright/ruff/bandit clean. 4b + release skipped
      (L2/Tier2, no auth/PII). 006-S13 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-025"
    event_id: "EV-044"
    timestamp: "2026-05-31T00:30:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      006 S14 attribute API polish bundle — final Epic 006 story. ADR-144. Added to model.py:
      append()/set_appends() (per-instance _instance_appends, merged in to_dict via _collect_appends
      to keep complexity <=10); make_hidden_if/make_visible_if (bool or self-predicate, chainable);
      only()/except_() subsets of to_dict; get_key_name()/get_key() (tuple for composite);
      qualify_column() from mapper local table; is_same()/is_not() (same type + non-null key);
      discard_changes() reverts dirty cols to committed_state. New HasUuids/HasUlids plain mixins:
      shared before_insert hook fills empty single-col string PK from type(target).new_unique_id()
      (uuid4 / 26-char Crockford ULID via os.urandom; _UniqueIdProvider Protocol keeps the hook
      typed). Exported HasUuids/HasUlids. Gates: arvel database+events 936 passed (+15);
      mypy/pyright/ruff/bandit clean. 4b + release skipped       (L2/Tier2, no auth/PII). 006-S14 DONE —
      EPIC 006 COMPLETE.
    handoff_id: null

  - work_item_id: "WI-arvel-026"
    event_id: "EV-045"
    timestamp: "2026-05-31T01:10:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      007 S1 morph map foundation — first Epic 007 story. ADR-145 (supersedes ADR-022 default token).
      New orm/morph_map.py: process-global _MorphState (mutated in place, no global rebinds);
      morph_map({alias: cls}, merge=) register/return; require_morph_map() strict toggle;
      get_morph_alias(cls) write-side (alias else short name, raises in strict); resolve_morph_class
      (read-side: map then Model.registry scan by short name, raises MorphMapError); reset_morph_map
      for test isolation (wired into reset_global_state autouse fixture). Model.get_morph_class()
      classmethod. Routed all morph write/read paths through get_morph_alias: MorphOne/MorphMany
      create+query, MorphToMany _owner_type, query.py morph-existence link_spec. Exported morph API
      from orm + database __init__. Gates: arvel database+events 948 passed (+12); mypy/pyright/ruff/
      bandit clean. Existing morph tests unchanged (short-name default preserved). 4b + release
      skipped (L2/Tier2, no auth/PII). 007-S1 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-027"
    event_id: "EV-046"
    timestamp: "2026-05-31T01:50:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      007 S2 MorphTo inverse relation. ADR-146 (builds on ADR-145 morph map). New in orm/morph.py:
      MorphTo(name=) descriptor + MorphToAccessor (awaitable: resolve_morph_class(token) then load by
      {name}_id; null discriminators -> None). associate(parent)/dissociate(): set/clear
      {name}_type (get_morph_alias) + {name}_id (parent.get_key()) together; associate primes eager
      cache; both return child. batch_load_morph_to(children, name, attr): group by {name}_type, one
      SELECT ... IN per distinct type, store parent on each child via set_eager_relation. Query
      builder: _RelationTarget gained morph_name:str + kind "morph_to"; _resolve_relation,
      _is_async_relation, _load_async_relation_path all handle it (morphTo is a leaf — no nested
      paths, parent type varies per row). Exported MorphTo/MorphToAccessor from orm. Tests verify
      query count (1 + per-type) via before_cursor_execute counter. Gates: arvel database+events 953
      passed (+5); mypy/pyright/ruff/bandit clean. 4b + release skipped (L2/Tier2, no auth/PII).
      007-S2 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-028"
    event_id: "EV-047"
    timestamp: "2026-05-31T02:40:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      007 S3 MorphOne/MorphMany query + eager integration. ADR-147. morph.py: MorphChildLink
      dataclass; MorphOne/MorphMany got __set_name__, name/related_model props, link_spec(owner_alias)
      and accessors now read the eager cache (MorphOne -> first, MorphMany -> list). New
      batch_load_morph_children: one WHERE {name}_type=alias AND {name}_id IN (pks) (+constraint),
      grouped to each owner via set_eager_relation. query.py: _RelationTarget.morph_child_link + kind
      "morph_child" wired into _resolve_relation, _exists_subquery, _count_subquery (extracted
      _morph_child_count_subquery for C901), _is_async_relation, _load_async_relation_path (with
      nested-path recursion + _constraint_where helper). Promoted is_async_relation /
      load_async_relation_path to public; Model.load() now splits SA vs async relations and batch-
      loads the latter into the eager cache. Exported MorphChildLink. Gates: arvel database+events
      960 passed (+7); mypy/pyright/ruff/bandit clean. 4b + release skipped (L2/Tier2). 007-S3 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-029"
    event_id: "EV-048"
    timestamp: "2026-05-31T03:30:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      007 S4 morphedByMany (inverse morph pivot). ADR-148. morph_to_many.py: MorphedByMany descriptor
      + MorphedByManyAccessor + MorphedByManyLink. Inverse pins {name}_type to the related model's
      alias and joins {name}_id to the related PK (string-cast); owner PK lives in the related_key FK
      column. Accessor: all/attach/detach/toggle/sync. Related model accepts a lambda: thunk (resolved
      lazily) for forward refs. query.py: new "mbm" kind wired into _resolve_relation,
      _is_async_relation, _batch_load_async (one WHERE type=alias AND owner_fk IN (pks), grouped per
      owner), _exists_subquery, _count_subquery, _load_async_relation_path. _exists_subquery and
      _count_subquery refactored into dispatchers over per-kind helpers (_pivot_exists_select,
      _sa/_mtm/_btm/_mbm_count_subquery) to stay under C901/PLR0911. Fixed a cast name collision
      (sqlalchemy.cast shadowed typing.cast) by aliasing to sa_cast. Exported MorphedByMany/Accessor/
      Link. Gates: arvel database+test_events 967 passed (+7); mypy/pyright/ruff/bandit clean. 4b +
      release skipped (L2/Tier2). 007-S4 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-030"
    event_id: "EV-049"
    timestamp: "2026-05-31T04:20:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      007 S5 has-one-of-many (latest/oldest/of_many). ADR-149. Two surfaces: (1) method style —
      _OfMany base shared by HasMany/HasOne adds latest_of_many/oldest_of_many/of_many (order by
      column + PK tiebreaker, first()); (2) descriptor HasOneOfMany (orm/has_one_of_many.py) +
      accessor + HasOneOfManyLink + batch_load_one_of_many (one grouped subquery: SELECT fk,
      AGG(col) GROUP BY fk joined back; ties broken by larger PK). Query builder "one_of_many" kind
      wired into _resolve_relation, _is_async_relation, _load_async_relation_path. Refactored
      _resolve_relation -> _resolve_descriptor_relation/_resolve_morph_descriptor and
      _load_async_relation_path -> _load_morph_to/_morph_child/_one_of_many_path helpers to satisfy
      C901/PLR0911. Exported HasOneOfMany/Accessor/Link. Gates: arvel       database+test_events 973 passed
      (+6); mypy/pyright/ruff/bandit clean. 4b + release skipped (L2/Tier2). 007-S5 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-031"
    event_id: "EV-050"
    timestamp: "2026-05-31T05:00:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      007 S6 chaperone (inverse parent hydration). ADR-150. QueryBuilder.chaperone(relation=None)
      marks a with_() closure; with_() runs the closure once on a probe builder, reads the flag, and
      records _Chaperone(head, inverse, uselist). Inverse resolution: explicit name -> back_populates
      -> MANYTOONE inference on the child mapper (else UnknownRelationError at build time).
      _eager_load_async runs _apply_chaperones before async specs, calling
      sqlalchemy.orm.attributes.set_committed_value(child, inverse, parent) per loaded child — no
      backref event, no lazy query, identity preserved (child.post is parent). Scope: SA has-one/
      has-many. Note: SQLAlchemy's identity map already makes the inverse query-free within a session;
      chaperone is the *guarantee* of the exact instance. Tests: test_wi031_chaperone.py (6). Gates:
      arvel database 949 passed; mypy/pyright/ruff/bandit clean. 4b + release skipped (L2/Tier2).
      007-S6 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-032"
    event_id: "EV-051"
    timestamp: "2026-05-31T05:40:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      007 S7 polymorphic existence queries. ADR-151. where_has_morph(relation, types, constraint=None)
      OR's a per-type AND(commentable_type==alias, EXISTS(select type WHERE pk==commentable_id [+
      constraint])); alias via get_morph_alias so morph_map is honoured; each branch through
      apply_global_scopes; empty types -> false(). has_morph adds count-based per-type COUNT scalar
      subquery. where_morph_relation = sugar over where_has_morph. _morph_to_name resolves the MorphTo
      and raises UnknownRelationError otherwise. _MorphConstraint = Callable[[QueryBuilder, type],
      QueryBuilder]. Tests: test_wi032_morph_existence.py (7). Gates: arvel database 956 passed (+7);
      mypy/pyright/ruff/bandit clean. 4b + release skipped (L2/Tier2). 007-S7 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-033"
    event_id: "EV-052"
    timestamp: "2026-05-30T23:05:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      007 S8 relation-querying completeness. ADR-152. One recursive _has_predicate(model, path,
      constraint, operator, count) backs the family: leaf hop carries constraint + operator/count
      (>=1 -> plain EXISTS, else correlated COUNT via _count_op/_constrained_count_subquery),
      intermediate hops wrap a child EXISTS that ANDs the nested predicate, so
      where_has("posts.comments", ...) walks both hops; all subqueries through apply_global_scopes.
      where_has now takes operator+count and a constraint closure; doesnt_have negates the predicate
      (constrained doesnt_have = "no row matching constraint"). or_where_has/or_doesnt_have/
      or_where_relation OR the predicate onto WHERE. with_where_has = where_has(name, c) + with_(
      {name: c}) so the parent is filtered and its collection pre-filtered. where_belongs_to(parent,
      relation=None) constrains the local FK to the parent key; _belongs_to_relation_for scans the
      mapper for the MANYTOONE rel whose target matches the parent instance (isinstance), else
      UnknownRelationError. Tests: test_wi033_relation_querying.py (11). Gates: arvel database 967
      passed (+11); mypy/pyright/ruff/bandit clean. 4b + release skipped (L2/Tier2). 007-S8 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-034"
    event_id: "EV-053"
    timestamp: "2026-05-30T23:35:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      007 S9 relationship aggregate completeness. ADR-153. One _aggregate_column(model, target, agg,
      col, constraint) backs every aggregate: resolves via _relation_exists_select (pivot-aware for
      BTM/MTM/MBM), applies constraint + apply_global_scopes (soft-delete scoped), then count
      (_count_subquery / _constrained_count_subquery), exists (select.exists()), or
      func.<sum|avg|min|max>(related.col).scalar_subquery(). with_aggregate(relation, agg, col, alias,
      constraint) is the QueryBuilder entry; with_count/with_sum/with_avg/with_min/with_max/with_exists
      delegate. Relation string takes " as <alias>"; explicit alias= wins; default labels {rel}_count/
      {rel}_exists/{rel}_{agg}_{col}. Hydrated via existing __with_agg__ result path. Instance loaders:
      load_aggregate_for(instance, ...) selects the scalar for one row and caches it on the instance
      (object.__setattr__ under suppress); Model exposes load_count/load_sum/load_exists/load_aggregate.
      Replaced bespoke with_sum/with_max (plain-SA only) with the unified path; existing with_sum/
      with_max/with_count tests still pass. Tests: test_wi034_aggregate_completeness.py (11). Gates:
      arvel database 978 passed (+11); mypy/pyright/ruff/bandit clean. 4b + release skipped (L2/Tier2).
      007-S9 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-035"
    event_id: "EV-054"
    timestamp: "2026-05-31T00:05:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      007 S10 pivot ergonomics (BelongsToMany). ADR-154. Frozen PivotConfig(columns, timestamps,
      created_at, updated_at, accessor) carries per-relation settings; fluent with_pivot(*cols)/
      with_timestamps(created,updated)/as_(name) configure the descriptor at class definition and flow
      to every accessor. Read path (_fetch) selects configured pivot columns alongside the related
      model and attaches a SimpleNamespace under the accessor name (default "pivot"); all() preserves
      the eager-cache fast path (returns cached without re-query). attach fills created_at+updated_at,
      update_pivot bumps updated_at (datetime.now(UTC), only when unset); sync inherits via attach/
      update_pivot. order_by_pivot(col, dir) + where_pivot_in/_not_in/_between/_null(negate) are
      terminal async list[T] returns. create(pivot=, **attrs) builds related + attaches; save(instance,
      pivot=) persists if PK null then attaches. Deferred: custom Pivot model via using (ADR-154-06,
      not in AC). Fixed regression: all() must honour eager cache (test_eager_loading_pivot). Tests:
      test_wi035_pivot_ergonomics.py (6). Gates: arvel database 984 passed (+6); mypy/pyright/ruff/
      bandit clean. 4b + release skipped (L2/Tier2). 007-S10 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-036"
    event_id: "EV-055"
    timestamp: "2026-05-31T00:30:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      007 S11 relation defaults / eager control / cascade save. ADR-155. BelongsTo tracks fk_present
      (false when owner FK is null — short-circuits first() so it no longer matches an arbitrary row)
      and a _default spec; with_default(True|dict|callback) returns an empty/attributed/callback-filled
      instance when the FK is null or unmatched, a real parent always wins. Eager control: with_() no
      longer mutates _stmt — _register_eager records sync requests onto _eager_loads (async/pivot onto
      _async_eager), materialised in apply_global_scopes so without(*names) drops and with_only(*rels)
      clears+re-registers before the SELECT builds; relation head still validated eagerly via
      _validate_eager_head (UnknownRelationError fail-fast preserved). __touches__ tuple of parent
      accessor method names → Model._touch_parents after save() fetches each parent and touch()es its
      UPDATED_AT (zero-cost when empty). push() saves the model then walks loaded relationships in the
      identity map (skips unloaded) and push()es each related instance, cascading edits. Deferred:
      eager column selection with("posts:id,title") (ADR-155-04, not in AC). Tests:
      test_wi036_relation_defaults.py (10). Gates: arvel database 994 passed (+10); mypy/pyright/ruff
      clean. 4b + release skipped (L2/Tier2). 007-S11 DONE — epic 007 complete.
    handoff_id: null

  - work_item_id: "WI-arvel-037"
    event_id: "EV-056"
    timestamp: "2026-05-31T01:00:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      006 S8 Eloquent-style ModelCollection. ADR-156. New ModelCollection(Collection[T]) in
      arvel/database/collection.py; QueryBuilder.all()/get() return it for model rows (scalar +
      with_agg paths), dict/raw rows stay on plain Collection. Key-aware (get_key): model_keys, find,
      contains (predicate|model|key), only/except_, diff/intersect (override base id()-based). Batch
      load(*rels): async/pivot/morph via load_async_relation_path (batched across members), SA rels
      via one select(pk IN keys)+selectinload copied onto members; load_missing skips populated.
      to_query (WHERE pk IN keys), fresh(*rels) (capture keys → expire members → re-query in order,
      drops deleted), make_hidden/make_visible fan out. Exported from arvel.database. Cast list[Any]
      at construction (QueryBuilder T bound to QueryMixin, not Model). Tests:
      test_wi037_model_collection.py (7). Gates: arvel database 1001 passed (+7); full package 3792
      passed (2 pre-existing cwd-relative skeleton failures, unrelated); mypy/pyright/ruff clean. 4b +
      release skipped (L2/Tier2). 006-S8 DONE.
    handoff_id: null

  - work_item_id: "WI-arvel-038"
    event_id: "EV-057"
    timestamp: "2026-05-31T05:30:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      Backlog 002 (all stories) — arvel-auth-social companion package. S1 built-in providers
      (Google/GitHub/Microsoft/Apple) + generic OIDC over a shared OAuthProvider base with injectable
      httpx client; SocialAuthManager resolves configured providers, SocialProviderNotFound lists what
      is configured. S2 redirect/callback HTTP flow: /auth/{provider}/redirect sets state + PKCE
      cookies; /callback verifies state (422 on mismatch), exchanges code, links account, issues a
      session; provider errors redirect to SOCIAL_ERROR_REDIRECT_URL. S3 PKCE on by default
      (secrets.token_urlsafe(96), S256-only, per-provider opt-out). S4 OIDC auto-discovery with
      trailing-slash normalisation, OIDCDiscoveryError on failure, HTTPS-only guard
      (SOCIAL_ALLOW_HTTP_ISSUER dev opt-out, enforced in OIDCProvider.discover). S5 SocialAccount model
      (EncryptedJson tokens via AES-256-GCM/APP_KEY, UNIQUE(provider, provider_id), provider_id
      verbatim), SocialAccountLinker (verified-email linking, unverified placeholder email,
      DuplicateSocialAccount guard), auth:social:install publishes the migration. AuthService.issue_for
      added to core to mint a token pair post-social-login. Stage 4b folded in (Tier 3 / confidential):
      PKCE, encrypted token storage, HTTPS-only discovery, HttpOnly SameSite=Lax state cookie. Docs:
      docs/site/docs/social-auth.md; arvel[auth-social] extra. Tests: 34 package tests pass;
      mypy/pyright/ruff/bandit clean. 002 S1–S5 DONE — epic 002 complete.
    handoff_id: null

  - work_item_id: "WI-arvel-039"
    event_id: "EV-058"
    timestamp: "2026-05-31T06:30:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      Backlog 003 (all stories) — arvel-search companion package, Scout-style full-text search. S1
      Searchable mixin: declare __searchable__; lifecycle hooks (created/updated/restored/deleted)
      auto-wired at class definition sync to the engine; only declared columns are indexed (sensitive
      columns warn at class-def time); search_index_name() defaults to __tablename__; to_searchable_array
      includes the key plus declared columns. SEARCH_QUEUE_SYNC dispatches SearchIndexJob/SearchRemoveJob
      that re-load rows by key for committed-state indexing. S2 SearchBuilder fluent API: where/limit/
      offset/get/keys/count/raw/first/paginate; hydration preserves engine relevance order; query
      forwarded verbatim (no injection); SearchEngineNotConfigured on missing engine. S3 multi-driver
      via SEARCH_DRIVER: database (SQL ILIKE, bound params), meilisearch + elasticsearch (REST over
      httpx, no SDKs), with register_driver() for custom engines and UnknownSearchDriverError on bad
      names. S4 null (no-op) + collection (in-memory substring) drivers and Search.fake()/SearchFake
      (assert_indexed/assert_removed/assert_nothing_indexed/assert_indexed_count). Search facade with
      fake()/restore(). Docs: docs/site/docs/search.md; arvel[search] extra. Tests: 59 package tests
      pass; mypy/pyright/ruff clean. 003 S1–S4 DONE — epic 003 complete.
    handoff_id: null

  - work_item_id: "WI-arvel-040"
    event_id: "EV-059"
    timestamp: "2026-05-31T07:30:00+03:00"
    from_stage: "execution"
    to_stage: "operations"
    role: "qa-engineer"
    result: "pass"
    notes: >
      Backlog 004 (all stories) — arvel-audit companion package: automatic audit trail +
      fluent activity log. S1 Auditable mixin auto-wires created/updating/updated/deleting/deleted
      at class definition (install_audit_observers, idempotent; re-asserted in AuditServiceProvider.boot
      via wire_all_auditable). Records AuditEntry in the same transaction: created => new={all cols}/
      old={}; updated => changed cols before+after (old captured on the pre-flush "updating" hook since
      SQLAlchemy clears history at flush); deleted => last values/new={}. __audit_redact__ masks columns
      as "***" before the row is written (never plaintext at rest); __audit_exclude__ omits columns.
      actor_id from Context.get("user_id"), None when unbound. AuditValues TypeDecorator stores old/new
      JSON, AES-256-GCM encrypted when AUDIT_ENCRYPT_VALUES=true (lazy APP_KEY resolve). S2 AuditLog query
      API (for_model/by_actor/action/since/until/get/first/count/paginate), InvalidAuditAction on bad
      action, ORM Paginator for pages; results are unredacted history (access control is the app's job).
      S3 activity(log, session).log().by().on().with_properties().save() => ActivityEntry;
      MissingActivityDescription without log(); subject's __audit_redact__ keys stripped from properties at
      save; ActivityQuery for_subject/by_causer. S4 audit:install publishes two migrations (audit_entries
      idx (model_type,model_id); activity_entries idx (subject_type,subject_id)+(causer_type,causer_id)),
      idempotent unless --force. Stage 4b folded in (confidential): redaction-before-write, optional
      at-rest encryption, non-null model_type/model_id, composite indexes. Docs: docs/site/docs/audit.md;
      arvel[audit] extra. arvel-audit/tests type-checked in isolation (hyphenated-package conftest
      collision, mirrors arvel-search). Tests: 24 package tests pass; mypy/pyright/ruff clean.
      004 S1–S4 DONE — epic 004 complete.
    handoff_id: null
