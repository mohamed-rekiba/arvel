schema_version: 1
events:
  - work_item: "WI-arvel-001"
    stage: "qa-pre"
    event: "exit"
    result: "pass"
    note: "Spec derived from research brief F2 + review; SPEC-1..7 authored with failing tests first."
    timestamp: "2026-06-08T01:05:00Z"
  - work_item: "WI-arvel-001"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Root-cause fixes: stream() fail-fast on pending eager loads (all kinds);
      RecursiveQueryBuilder.as_tree() now runs _eager_load_async like all();
      new EagerLoadNotStreamableError exported. No backward-compat shims.
    artifacts:
      - "packages/arvel/src/arvel/database/exceptions.py"
      - "packages/arvel/src/arvel/database/query.py"
      - "packages/arvel/src/arvel/database/__init__.py"
      - "packages/arvel/tests/database/test_stream_eager_load_guard.py"
    timestamp: "2026-06-08T01:25:00Z"
  - work_item: "WI-arvel-001"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      pytest packages/arvel/tests/database = 1137 passed + 7 new guard tests;
      mypy --strict (1040 files) clean; pyright 0 errors; ruff clean;
      live kit DB read sanity (13 visible + 1 draft; 14 live products).
    timestamp: "2026-06-08T01:45:00Z"
  - work_item: "WI-arvel-001"
    stage: "reverify"
    event: "exit"
    result: "pass"
    note: >-
      requesting-code-review re-verify: implementation confirmed correct; initial
      "With fixes" (test breadth + doc labels) resolved — guard tests now cover
      all eager buckets (selectin/async/tree/chaperone) + all()/as_tree() parity;
      docstring/section labels/assertions corrected.       Pre-existing recursive() eager
      carry-over recorded as tracked deferral (backlog 001 notes). Verdict: clean.
    timestamp: "2026-06-08T02:00:00Z"
  - work_item: "WI-arvel-002"
    stage: "qa-pre"
    event: "exit"
    result: "pass"
    note: >-
      Spec derived from research brief 002 (D1 reproduced: controllers singleton at
      mount -> calls 1,2,3) + review (Critical confirmed both adapters). SPEC-1..7
      authored; freshness tests written to fail against the singleton behavior.
    timestamp: "2026-06-08T04:40:00Z"
  - work_item: "WI-arvel-002"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Root-cause fix: MethodControllerAdapter.build() and _invokable_controller_adapter()
      now resolve a fresh instance per request (probe at mount only validates DI +
      reads receiver-free signature, then discarded). D2: DB-tx middleware except tuple
      parenthesized. No backward-compat shims.
    artifacts:
      - "packages/arvel/src/arvel/routing.py"
      - "packages/arvel/src/arvel/http/middleware/database_transaction.py"
      - "packages/arvel/tests/routing/test_wi002_per_request_controllers.py"
    timestamp: "2026-06-08T04:50:00Z"
  - work_item: "WI-arvel-002"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      pytest routing+http = 308 passed (incl. 4 new freshness tests + unchanged
      boot-time DI fail-fast); mypy --strict clean; pyright 0 errors; ruff clean.
      Kit ASGI boots (88 routes, all controllers resolve at mount); live E2E
      /api/i18n/en + /healthz return 200 with stable ETag across requests.
    timestamp: "2026-06-08T05:00:00Z"
  - work_item: "WI-arvel-002"
    stage: "reverify"
    event: "exit"
    result: "pass"
    note: >-
      requesting-code-review re-verify verdict: Ready. Minors folded in: added
      intentional-shared (instance()-bound) controller test, tightened spec wording
      on probe lifetime, checked backlog acceptance boxes. Dropped a redundant
      invokable unit test that reached into a private symbol (pyright reportPrivateUsage)
      — invokable freshness stays covered via HTTP. Final: 309 routing+http tests
      pass, mypy/pyright/ruff clean. Deferred: auth _mount_routes closure capture,
      per-request DI child scope, rate-limit header parity.
    timestamp: "2026-06-08T05:10:00Z"
  - work_item: "WI-arvel-003"
    stage: "qa-pre"
    event: "exit"
    result: "pass"
    note: >-
      Spec from research brief 003 (F1 escalation confirmed in grant_permission /
      revoke_permission / revoke_role vs guarded assign_role; F2 redundant user fetch
      confirmed: me() returns the configured user model the guard is built with).
      SPEC-1..7 authored; escalation + no-redundant-query tests written to fail first.
    timestamp: "2026-06-08T05:25:00Z"
  - work_item: "WI-arvel-003"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Root-cause fixes: make_permission_guard uses the require_auth user directly via an
      isinstance fast path (no second identical SELECT), reload kept only for mismatched
      models; kit AdminUsersController now requires the actor to hold a permission to
      grant/revoke it and to outrank a role to revoke it (mirrors assign_role). No shims.
    artifacts:
      - "packages/arvel/src/arvel/auth/guards/__init__.py"
      - "packages/arvel/tests/auth/test_guards.py"
      - "kits/arvel-ecommerce-kit/backend/app/http/controllers/admin/users.py"
      - "kits/arvel-ecommerce-kit/backend/tests/unit/test_057_authz_escalation_guard.py"
    timestamp: "2026-06-08T05:40:00Z"
  - work_item: "WI-arvel-003"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      pytest packages/arvel/tests/auth = 171 passed (incl. 2 new guard query tests);
      kit tests/unit/test_057 = 5 passed (escalation deny + pass-through); arvel-permission
      = 75 passed (no RBAC regression); full mypy --strict (1041 files) + pyright clean;
      ruff clean. Deferred (tracked): F3 per-role permissions.all() N+1, F4 password_resets
      schema drift, F5 access-JWT revocation + process-local throttle, F6 auth
      _mount_routes boot capture, F7 monorepo config-shadow breaking ~33 kit unit tests.
    timestamp: "2026-06-08T05:55:00Z"
  - work_item: "WI-arvel-003"
    stage: "reverify"
    event: "exit"
    result: "pass"
    note: >-
      requesting-code-review re-verify verdict: Ready. Guard fast path preserves the old
      None/permission 403 semantics (me() never returns None, so isinstance-False reload is
      safe); kit revoke_role now resolves role level first (consistent with assign_role).
      Docs synced (permission.md primitive-vs-policy warning), backlog epic 003 boxes
      checked. Final: 246 auth+permission tests + 5 kit authz tests pass, mypy --strict
      (1041) + pyright + ruff clean. F7 (config-shadow) flagged as a test-infra WI.
    timestamp: "2026-06-08T06:05:00Z"
  - work_item: "WI-arvel-004"
    stage: "planning"
    event: "enter"
    result: "pass"
    note: >-
      Module 4 (validation/FormRequest). deep-research brief 004 + code-review scoped two
      defects: F1 reset_password emitted Pydantic-style {loc, msg, type} details while the
      rest of the framework uses canonical {field, issue}; F2 the TestResponse helper could
      not parse Arvel's own error-bag envelope ({"error": {"details": [{"field": ...}]}}).
      Deferred (tracked): F3-F6 (rules-layer architecture, nullable parity, unknown-rule
      soft-fail, exists/unique session error).
    timestamp: "2026-06-08T06:00:00Z"
  - work_item: "WI-arvel-004"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Fix A: reset_password ValidationException now carries {"field": "token", "issue": ...}
      so the detail shape matches every other validation error (error-bag and RFC 7807
      handlers both render it cleanly). Fix B: _extract_error_fields recognises the Arvel
      error-bag and RFC 7807 {field} entries alongside FastAPI loc and Laravel errors-map,
      refactored into small parsers to stay under the complexity limit. No shims.
    artifacts:
      - "packages/arvel/src/arvel/auth/http/controller.py"
      - "packages/arvel/src/arvel/testing/response.py"
      - "packages/arvel/tests/testing/test_response.py"
      - "packages/arvel/tests/test_auth/integration/test_controller.py"
    timestamp: "2026-06-08T06:20:00Z"
  - work_item: "WI-arvel-004"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Full arvel suite = 4287 passed (3 deselected); targeted auth-controller + testing
      = 51 passed incl. new canonical-detail integration test and error-bag helper test;
      mypy --strict + pyright + ruff all clean on changed files. Pre-existing harness
      quirk noted: password_resets table only materialises under full-module ordering
      (F4 schema drift), so the new test runs alongside the module's other reset tests.
    timestamp: "2026-06-08T06:30:00Z"
  - work_item: "WI-arvel-005"
    stage: "planning"
    event: "enter"
    result: "pass"
    note: >-
      Module 5 (http/resources). deep-research brief 005 + empirical repro confirmed F1:
      JsonResource.when_loaded only checked resource.__dict__, but Arvel's async relations
      (has-many, belongs-to-many, morph-to-many) cache eager results under
      __arvel_eager_relations__, so the documented with_(rel) -> when_loaded(rel) pattern
      silently dropped the relation. Deferred: F2 missing whenNotNull/with()/withoutWrapping,
      F3 no auto-resolve of bare resource return, F4 ResourceResponse can't encode
      datetime/Decimal, F5 additional() overwrites paginator meta, F6 kit dead-code
      resources, F7 schema ClassVar not wired to response_model.
    timestamp: "2026-06-08T06:40:00Z"
  - work_item: "WI-arvel-005"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      when_loaded now consults the eager cache (read by name as __arvel_eager_relations__,
      no arvel.database import — preserves the module's layering rule) after the __dict__
      check. Returns the cached relation; documented eager-load -> serialize works for every
      relation type. New real-model test (MorphToMany via with_) covers the gap the prior
      mock-based parity tests missed. No shims.
    artifacts:
      - "packages/arvel/src/arvel/http/resources.py"
      - "packages/arvel/tests/http/test_wi005_when_loaded_eager.py"
    timestamp: "2026-06-08T06:55:00Z"
  - work_item: "WI-arvel-005"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Full arvel suite = 4290 passed (3 deselected), +3 new resource tests, no regression;
      http + eager-pivot suites = 202 passed; mypy --strict + pyright + ruff clean on
      changed files. No new arvel.database import in http/resources.py (layering preserved).
      Docs note clarified that when_loaded covers SQLAlchemy and async relations.
    timestamp: "2026-06-08T07:05:00Z"
  - work_item: "WI-arvel-006"
    stage: "planning"
    event: "enter"
    result: "pass"
    note: >-
      Module 6 (storage/filesystem). deep-research brief 006 confirmed F1: a missing file raised
      different, unrelated exceptions per disk — MemoryDriver raised StorageFileNotFoundError
      (StorageDriverError only), LocalDriver raised the builtin FileNotFoundError (get explicitly,
      size via Path.stat). The two were disjoint OSError branches, so disk-agnostic callers had no
      single exception to catch. Two prior suspicions cleared as non-issues: LocalDriver path
      traversal (false positive — startswith(root + "/") blocks sibling-prefix bypass) and
      `except A, B:` syntax (valid under Python 3.14 PEP 758). Deferred: F2 size on directory,
      F3 url() no validation (matches Laravel), F4 cloud-driver missing-key paths.
    timestamp: "2026-06-08T07:20:00Z"
  - work_item: "WI-arvel-006"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      StorageFileNotFoundError now subclasses the builtin FileNotFoundError (and StorageDriverError);
      LocalDriver.get/size raise it explicitly, mirroring MemoryDriver. One type on every disk,
      catchable as the storage taxonomy or the builtin — so existing catchers (serve route 404,
      media _delete_quiet, console, maintenance) keep working with zero call-site churn. Tests
      unified to assert StorageFileNotFoundError; added local size-missing + builtin-catchability
      tests. No shims.
    artifacts:
      - "packages/arvel/src/arvel/storage/exceptions.py"
      - "packages/arvel/src/arvel/storage/drivers/local.py"
      - "packages/arvel/tests/storage/test_drivers.py"
      - "docs/site/docs/features/storage.md"
    timestamp: "2026-06-08T07:35:00Z"
  - work_item: "WI-arvel-006"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      storage + media suites = 834 passed; mypy --strict + pyright + ruff clean on changed files.
      MRO verified: [StorageFileNotFoundError, FileNotFoundError, StorageDriverError, OSError, ...]
      — isinstance true for builtin, StorageDriverError, and OSError. Docs storage.md documents the
      unified missing-file contract.
    timestamp: "2026-06-08T07:45:00Z"
  - work_item: "WI-arvel-007"
    stage: "planning"
    event: "enter"
    result: "pass"
    note: >-
      Module 7 (cache). deep-research brief 007 + empirical repro confirmed two coupled defects:
      (C2) CacheManager DATABASE driver built a throwaway :memory: engine and never created the
      table — first put/get raised "no such table: cache_entries"; (C1) the published migration
      created table `cache`(expiration) but DatabaseStore reads `cache_entries`(expires_at). The
      documented publish-migrate-use path could never work. Cleared as non-defects: remember not
      single-flight (matches Laravel), whole-table/dir flush (matches Laravel), _get_or_create_mutex
      "race" (asyncio false positive — sync, no await). Deferred: F3 ttl<=0 cross-store
      inconsistency, F4 non-atomic RateLimiter, F5 parity-additive APIs, F6 assert_stored prefix.
    timestamp: "2026-06-08T08:00:00Z"
  - work_item: "WI-arvel-007"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      DATABASE branch now resolves DB.session_maker_for() (the app's default connection) instead of
      a throwaway :memory: engine; raises a clear "DB not configured" error when unset. Migration
      create_cache_table.py now creates cache_entries(key VARCHAR(255) PK, value TEXT, expires_at
      INTEGER) matching the ORM. New driver tests cover app-connection use, the unconfigured-DB
      error, and migration/ORM table-shape match; framework-migration table list updated to
      cache_entries. No shims.
    artifacts:
      - "packages/arvel/src/arvel/cache/__init__.py"
      - "packages/arvel/src/arvel/cache/migrations/create_cache_table.py"
      - "packages/arvel/tests/cache/test_cache_manager_drivers.py"
      - "packages/arvel/tests/database/test_framework_migrations.py"
      - "docs/site/docs/features/cache.md"
    timestamp: "2026-06-08T08:15:00Z"
  - work_item: "WI-arvel-007"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Full arvel suite = 4295 passed (3 deselected), no regression; cache suite 151 passed;
      mypy --strict + pyright + ruff clean on changed files. Docs cache.md replaces the
      "not yet wired to your application database" warning with publish-migrate-use instructions.
    timestamp: "2026-06-08T08:25:00Z"
  - work_item: "WI-arvel-008"
    stage: "planning"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 8 (queue) deep-research: F1 = Worker._process_one caught CancelledError as a job
      failure (re-queue/DLQ + swallowed cancellation). Verified empirically that on Python 3.14 a
      job timeout surfaces as TimeoutError, not CancelledError, so the CancelledError->TimeoutError
      conversion was dead code and CancelledError only means external cancellation. F2 (delete-on-pop
      / no visibility timeout) and F3 (taskiq pop_blocking ignores timeout) deferred. Spec:
      WI-arvel-008-worker-cancellation-contract.md.
    artifacts:
      - ".context/research/008-queue.md"
      - "docs/pipeline/specs/WI-arvel-008-worker-cancellation-contract.md"
    timestamp: "2026-06-08T08:40:00Z"
  - work_item: "WI-arvel-008"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      worker.py: split `except (Exception, asyncio.CancelledError)` into
      `except asyncio.CancelledError: raise` + `except Exception as exc`, and removed the dead
      CancelledError->TimeoutError("Job timed out") conversion in the DLQ branch. New test
      test_external_cancellation_propagates_and_does_not_fail_job asserts CancelledError propagates,
      jobs_dead==0, jobs_retried==0, DLQ count()==0. Existing timeout/retry/DLQ tests unaffected
      (timeout -> TimeoutError -> except Exception). No shims.
    artifacts:
      - "packages/arvel/src/arvel/queue/worker.py"
      - "packages/arvel/tests/test_queue/test_047_queue_reliability.py"
      - "docs/site/docs/features/queues.md"
    timestamp: "2026-06-08T08:55:00Z"
  - work_item: "WI-arvel-008"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Queue suite = 231 passed; mypy --strict + pyright + ruff clean on changed files. Docs
      queues.md notes worker cancellation is not a job failure. F2/F3 tracked as deferred.
    timestamp: "2026-06-08T09:05:00Z"
  - work_item: "WI-arvel-009"
    stage: "planning"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 9 (events/notifications) deep-research: C1 = NotificationJob._import_class ran
      importlib.import_module on dotted paths from the (untrusted) queue payload — the only place
      in the queue layer bypassing the allowlist boundary (JobRegistry / ListenerJob's
      EventRegistry+ListenerRegistry). Insecure deserialization (A08): arbitrary module import =
      import-time side-effect gadget. Verified the Notifiable __init_subclass__ chain fires through
      the ORM Model MRO. C2 (queued notification state loss), morph key, txn boundary, dispatcher
      log-swallow deferred. Spec: WI-arvel-009-queued-notification-allowlist.md.
    artifacts:
      - ".context/research/009-events-notifications.md"
      - "docs/pipeline/specs/WI-arvel-009-queued-notification-allowlist.md"
    timestamp: "2026-06-08T09:20:00Z"
  - work_item: "WI-arvel-009"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Added NotificationRegistry (notification.py) + NotifiableRegistry (notifiable.py) via
      __init_subclass__, mirroring Event/Listener. NotificationJob.handle now resolves both classes
      via _resolve() against the registries; removed _import_class; new
      UnregisteredNotificationClassError raised on a miss (no import). Tests: unregistered class is
      rejected without touching sys.modules; registry-resolution test replaces the old _import_class
      test; _QueuedNotifiable now subclasses Notifiable. No shims.
    artifacts:
      - "packages/arvel/src/arvel/notifications/notification.py"
      - "packages/arvel/src/arvel/notifications/notifiable.py"
      - "packages/arvel/src/arvel/notifications/notification_job.py"
      - "packages/arvel/src/arvel/notifications/exceptions.py"
      - "packages/arvel/tests/test_queue/test_047_queue_reliability.py"
      - "packages/arvel/tests/test_notifications/test_channels_and_notifiable_more.py"
      - "docs/site/docs/features/notifications.md"
    timestamp: "2026-06-08T09:35:00Z"
  - work_item: "WI-arvel-009"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Full arvel suite = 4297 passed (3 deselected), no regression (Notifiable.__init_subclass__
      doesn't disturb model creation); notifications/queue/events 303 passed; mypy --strict +
      pyright + ruff clean on changed files. Docs notifications.md documents the allowlist contract.
    timestamp: "2026-06-08T09:45:00Z"
  - work_item: "WI-arvel-010"
    stage: "planning"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 10 (mail) deep-research: C1 = SmtpMailDriver._build_message attaches only att.data, so
      documented path-based attachments are silently dropped (att.mime also ignored; filename
      interpolated raw). C3 (CRLF header injection) cleared as false positive — Python's email
      generator raises HeaderParseError on embedded newlines. C2 (sync render in async path), H1-H4,
      and parity-additive gaps deferred. Spec: WI-arvel-010-smtp-path-attachments.md.
    artifacts:
      - ".context/research/010-mail.md"
      - "docs/pipeline/specs/WI-arvel-010-smtp-path-attachments.md"
    timestamp: "2026-06-08T10:00:00Z"
  - work_item: "WI-arvel-010"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      smtp.py: extracted _build_attachment(att) — reads att.path when att.data is None (raises
      MailException if neither), sets MIMEBase maintype/subtype from att.mime, sets filename via
      add_header(..., filename=) (RFC 2231). New tests: path attachment read+attached with correct
      content-type; empty attachment raises. No shims.
    artifacts:
      - "packages/arvel/src/arvel/mail/drivers/smtp.py"
      - "packages/arvel/tests/test_mail/drivers/test_smtp_driver.py"
      - "docs/site/docs/features/mail.md"
    timestamp: "2026-06-08T10:15:00Z"
  - work_item: "WI-arvel-010"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Mail suite = 48 passed; mypy --strict + pyright + ruff clean on changed files. Docs mail.md
      notes path is read, Content-Type from mime, and one of path/data required. C2/H/parity gaps
      and the kit template-publish gap tracked as deferred.
    timestamp: "2026-06-08T10:25:00Z"
  - work_item: "WI-arvel-011"
    stage: "planning"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 11 (session) deep-research: C1 = SessionData.regenerate() rotates the id but never
      destroys the old store record, and StartSession has no destroy call — so a returning visitor's
      pre-login session stays valid after login (contradicts the "prevent fixation" comment; Laravel
      uses migrate(true)). Reproduced: guest cart under id A survives login→B. except A,B PEP 758
      syntax ruled out (requires-python>=3.14). Cookie-driver/StartSession integration, invalidate(),
      and flash get() shadowing deferred. Spec: WI-arvel-011-session-regenerate-destroy.md.
    artifacts:
      - ".context/research/011-sessions.md"
      - "docs/pipeline/specs/WI-arvel-011-session-regenerate-destroy.md"
    timestamp: "2026-06-08T10:30:00Z"
  - work_item: "WI-arvel-011"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      data.py: regenerate() queues the old id in a non-serialized _pending_destroy list;
      drain_pending_destroy() returns+clears; __init__/from_dict init the list. middleware.py:
      StartSession destroys each drained id after writing the rotated session. New tests: regenerate
      queues old id, drain is one-shot, queue not serialized, and middleware integration destroys the
      old record. No shims.
    artifacts:
      - "packages/arvel/src/arvel/session/data.py"
      - "packages/arvel/src/arvel/session/middleware.py"
      - "packages/arvel/tests/session/test_session_data.py"
      - "packages/arvel/tests/session/test_middleware.py"
      - "docs/site/docs/features/session.md"
    timestamp: "2026-06-08T10:40:00Z"
  - work_item: "WI-arvel-011"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Full arvel suite = 4303 passed (3 deselected), no regression; session + auth-safety 80 passed;
      mypy --strict + pyright + ruff clean on the session module. Docs session.md notes regenerate()
      destroys the old record on response (Laravel migrate(true)). Cookie-driver integration,
      invalidate(), and flash shadowing tracked as deferred.
    timestamp: "2026-06-08T10:50:00Z"
  - work_item: "WI-arvel-012"
    stage: "planning"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 12 (broadcasting) deep-research: C1 = ShouldBroadcast.broadcast_with() returns
      model_dump() (python mode), so datetime/UUID/Decimal stay Python objects; every real driver
      json.dumps the payload and raises BroadcastDriverError. Reproduced with an OrderShipped event
      (UUID/Decimal/datetime). Reverb WS/auth already Stage-4b hardened. Auth presence/private
      discriminator (by return-type not channel name), toOthers(), and Pusher auto-build deferred.
      Spec: WI-arvel-012-broadcast-payload-json-safe.md.
    artifacts:
      - ".context/research/012-broadcasting.md"
      - "docs/pipeline/specs/WI-arvel-012-broadcast-payload-json-safe.md"
    timestamp: "2026-06-08T10:55:00Z"
  - work_item: "WI-arvel-012"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      should_broadcast.py: broadcast_with() now calls model_dump(mode="json") so the default payload
      is JSON-safe (datetime→ISO, UUID→str, Decimal→str); still routed through _as_payload_mapping.
      New test: rich-type event yields json.dumps-able payload; plain-int test unchanged. No shims.
    artifacts:
      - "packages/arvel/src/arvel/broadcasting/should_broadcast.py"
      - "packages/arvel/tests/broadcasting/test_should_broadcast.py"
      - "docs/site/docs/features/broadcasting.md"
    timestamp: "2026-06-08T11:05:00Z"
  - work_item: "WI-arvel-012"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Broadcasting suite = 91 passed; full arvel suite = 4304 passed (3 deselected), no regression;
      mypy --strict + pyright + ruff clean on changed files. Docs broadcasting.md notes the default
      uses model_dump(mode="json"). Auth discriminator, toOthers(), Pusher auto-build deferred.
    timestamp: "2026-06-08T11:15:00Z"
  - work_item: "WI-arvel-013"
    stage: "planning"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 13 (scheduling) audit. C1 confirmed via deterministic repro: SchedulerKernel onOneServer
      election lock used a time-less key (scheduler:onserver:{name}) and relied on TTL expiry, so a
      lock acquired in minute N blocked every server until TTL — any task with TTL >= run interval
      fired once then went dark. Laravel keys it per minute (mutexName().format('Hi')). Builder
      frequency/constraint sugar and tick-drift-vs-cron deferred (parity-additive / design).
      Spec: WI-arvel-013-onserver-lock-per-minute.md.
    artifacts:
      - ".context/research/013-scheduling.md"
      - "docs/pipeline/specs/WI-arvel-013-onserver-lock-per-minute.md"
    timestamp: "2026-06-08T11:55:00Z"
  - work_item: "WI-arvel-013"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      kernel.py: _run_one now takes the tick's now and folds the due minute
      (now.strftime("%Y%m%d%H%M")) into the election key scheduler:onserver:{name}:{slot}, so the lock
      rotates per minute — dedupes servers within a minute, never blocks the next run. Lock still never
      released (TTL cleanup), matching Laravel. New tests: lock_rotates_per_minute (3/3 over 3 mins),
      two_servers_same_minute_still_elect_one_winner. Folded-in: session test_middleware switched off
      private _store to public store.read() to keep pyright clean. No shims.
    artifacts:
      - "packages/arvel/src/arvel/scheduling/kernel.py"
      - "packages/arvel/tests/scheduling/test_scheduler_kernel.py"
      - "packages/arvel/tests/session/test_middleware.py"
      - "docs/site/docs/features/scheduling.md"
    timestamp: "2026-06-08T12:05:00Z"
  - work_item: "WI-arvel-013"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Scheduling suite = 56 passed; full arvel suite = 4306 passed (3 deselected), no regression;
      mypy --strict (1042 files) + pyright (0 errors) + ruff clean. Docs scheduling.md explains the
      per-minute election scope and shared-cache requirement. Builder sugar + tick-drift deferred.
    timestamp: "2026-06-08T12:10:00Z"
  - work_item: "WI-arvel-014"
    stage: "planning"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 14 (console/CLI) audit. C1 confirmed systemic: 9 command callbacks (cache:clear,
      cache:forget, schedule:run, db:show, db:table, queue:clear, queue:prune-failed, queue:restart,
      auth:clear-resets) call asyncio.run inside the Typer callback, nesting on the entrypoint's live
      asyncio.run(async_main()) loop -> RuntimeError in-project (reproduced for cache:clear/forget/
      schedule:run in the kit). Coupled: async_main dropped a deferred coro's typer.Exit/Abort, so an
      honest failure escaped as a traceback. cache:clear/forget/queue:restart also missed requires=CACHE.
      Application.run()->handle() bypass deferred (separate WI). Spec: WI-arvel-014-cli-single-event-loop.md.
    artifacts:
      - ".context/research/014-console.md"
      - "docs/pipeline/specs/WI-arvel-014-cli-single-event-loop.md"
    timestamp: "2026-06-08T12:15:00Z"
  - work_item: "WI-arvel-014"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      9 command callbacks converted asyncio.run(coro) -> schedule_async(coro), moving post-processing
      and the typer.Exit raise into an inner async fn. entrypoint.async_main now maps a deferred
      typer.Exit -> SystemExit(code) and typer.Abort -> SystemExit(1). requires=CACHE added to
      cache:clear, cache:forget, queue:restart. Folded-in: fixed a Python-2 "except A, B:" SyntaxError
      in tests/console/conftest.py (console suite was uncollectable from HEAD); switched cache/
      introspection/queue tests that assert deferred behavior to the invoke_async helper. No shims.
    artifacts:
      - "packages/arvel/src/arvel/console/commands/cache_commands.py"
      - "packages/arvel/src/arvel/console/commands/schedule_run.py"
      - "packages/arvel/src/arvel/console/commands/db_show.py"
      - "packages/arvel/src/arvel/console/commands/db_table.py"
      - "packages/arvel/src/arvel/console/commands/queue_clear.py"
      - "packages/arvel/src/arvel/console/commands/queue_prune_failed.py"
      - "packages/arvel/src/arvel/console/commands/queue_restart.py"
      - "packages/arvel/src/arvel/console/commands/auth_clear_resets.py"
      - "packages/arvel/src/arvel/console/entrypoint.py"
      - "packages/arvel/tests/console/test_wi_014_cli_async_loop.py"
      - "packages/arvel/tests/console/conftest.py"
    timestamp: "2026-06-08T12:30:00Z"
  - work_item: "WI-arvel-014"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Console suite = 721 passed; full arvel suite green (2 pre-existing cwd/DB-dependent skeleton
      failures confirmed failing on clean HEAD, unrelated to console); mypy --strict (1043 files) +
      pyright (0 errors, repo root) + ruff check + ruff format clean. Docs cli/commands.md notes the
      single-event-loop contract for command authors. Application.run()->handle() bypass + stale
      artisan.md pointer deferred.
    timestamp: "2026-06-08T12:35:00Z"
  - work_item: "WI-arvel-015"
    stage: "planning"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 15 (encryption) audit. C1 confirmed via repro: Encrypter.decrypt_string ran
      base64.b64decode(payload) outside its try/except, so a non-base64 payload leaked a raw
      binascii.Error instead of the documented DecryptionError. Sibling EncryptedType already funnels
      all bad input into DecryptionError; Laravel's Encrypter::decrypt raises DecryptException for any
      invalid payload. A Crypt-facade caller decrypting untrusted input and catching DecryptionError
      gets an uncaught 500 (A10). decrypt() JSON + from_app_key bad-key deferred (parity-additive /
      operator config). Spec: WI-arvel-015-decrypt-error-contract.md.
    artifacts:
      - ".context/research/015-encryption.md"
      - "docs/pipeline/specs/WI-arvel-015-decrypt-error-contract.md"
    timestamp: "2026-06-08T12:40:00Z"
  - work_item: "WI-arvel-015"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      encrypter.py: wrapped base64.b64decode(payload) in try/except ValueError (binascii.Error
      subclasses ValueError) raising DecryptionError, so decrypt_string raises the documented type for
      every malformed payload (matches EncryptedType + Laravel). New tests/encryption/ package with
      round-trip + parametrized malformed/wrong-version/wrong-key/tampered cases (Encrypter had no
      direct unit coverage before). No shims.
    artifacts:
      - "packages/arvel/src/arvel/encryption/encrypter.py"
      - "packages/arvel/tests/encryption/__init__.py"
      - "packages/arvel/tests/encryption/test_wi_015_decrypt_error_contract.py"
      - "docs/site/docs/features/encryption.md"
    timestamp: "2026-06-08T12:50:00Z"
  - work_item: "WI-arvel-015"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      encryption + crypt-facade + encrypted-casts suites green (8 new encryption tests); full arvel
      suite green; mypy --strict (1045 files) + pyright (0 errors) + ruff check + ruff format clean.
      Docs encryption.md states DecryptionError covers malformed base64, unknown version, wrong key,
      and tampering. decrypt() JSON + from_app_key deferred.
    timestamp: "2026-06-08T12:55:00Z"
  - work_item: "WI-arvel-016"
    stage: "planning"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 16 (hashing) audit. C1 confirmed via repro: Hash.check/checkpw hardcoded argon2 verify,
      so a bcrypt hash (Hash.make_bcrypt or an imported Laravel $2y$ column) raised InvalidHashError,
      swallowed by the broad except → a correct password silently returned False (every bcrypt user
      locked out; session guard + login both verify via Hash.check). C2 coupled: needs_rehash raised
      InvalidHashError on a bcrypt hash. Laravel's Hash::check is algorithm-agnostic (password_verify
      auto-detects) and needsRehash flags an algorithm mismatch. Spec: WI-arvel-016-hash-cross-algorithm.md.
    artifacts:
      - ".context/research/016-hashing.md"
      - "docs/pipeline/specs/WI-arvel-016-hash-cross-algorithm.md"
    timestamp: "2026-06-08T13:05:00Z"
  - work_item: "WI-arvel-016"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      hash.py: check returns False for an empty hash, dispatches a bcrypt-prefixed hash
      ($2a$/$2b$/$2y$) to bcrypt.checkpw and everything else to argon2 verify; missing bcrypt extra
      degrades to a non-match (no crash). needs_rehash returns True for any bcrypt hash (argon2id is
      the default, so login upgrades it) and defers argon2 hashes to check_needs_rehash. Extracted
      _load_bcrypt() shared by make_bcrypt + _check_bcrypt. New tests/auth/test_wi_016_hash_cross_algorithm.py
      (no bcrypt-check coverage existed). No shims.
    artifacts:
      - "packages/arvel/src/arvel/facades/hash.py"
      - "packages/arvel/tests/auth/test_wi_016_hash_cross_algorithm.py"
      - "docs/site/docs/features/authentication.md"
    timestamp: "2026-06-08T13:20:00Z"
  - work_item: "WI-arvel-016"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Auth suite 178 passed (7 new hash cross-algorithm tests + 13 existing hash tests green); full
      arvel suite green; mypy --strict (1046 files) + pyright (0 errors) + ruff check + ruff format
      clean. Docs authentication.md notes Hash.check/needs_rehash are algorithm-aware and imported
      Laravel $2y$ hashes verify and auto-upgrade. HashConfig wiring + make(**kwargs) deferred.
    timestamp: "2026-06-08T13:25:00Z"
  - work_item: "WI-arvel-017"
    stage: "planning"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 17 (container/DI) audit. C1 confirmed via repro: _aresolve delegated auto-wiring and
      concrete-class bindings to the sync _instantiate (allow_async=False), so amake(Service) raised
      BindingResolutionError wrapping AsyncBindingError as soon as a transitive dep was bound to an
      async factory (Service->Repo->Db, Db async-bound). amake's whole purpose (async resolution at
      any depth) only worked when the async binding was the root. C2 coupled: the async path had no
      cycle guard (sync _resolve has one), so cyclic async graphs hit RecursionError instead of
      CircularDependencyError. AsyncSession TRANSIENT-vs-scoped + alias()/extend() deferred. Spec:
      WI-arvel-017-async-deep-resolution.md.
    artifacts:
      - ".context/research/017-container.md"
      - "docs/pipeline/specs/WI-arvel-017-async-deep-resolution.md"
    timestamp: "2026-06-08T13:45:00Z"
  - work_item: "WI-arvel-017"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      container.py: added _ainstantiate (async twin of _instantiate; each constructor dep resolved via
      await _aresolve, threading path). _aresolve now strips generic-alias origins, guards
      `if abstract in path: raise CircularDependencyError`, and routes the auto-wire fallback, the
      concrete-class binding branch, and the contextual concrete-class branch through _ainstantiate.
      Sync make, async factories (no-arg), and scope/singleton caching unchanged. New
      tests/container/test_wi_017_async_deep_resolution.py (5 cases). No shims.
    artifacts:
      - "packages/arvel/src/arvel/container/container.py"
      - "packages/arvel/tests/container/test_wi_017_async_deep_resolution.py"
      - "docs/site/docs/core-concepts/service-container.md"
    timestamp: "2026-06-08T14:00:00Z"
  - work_item: "WI-arvel-017"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Container suite 44 passed (5 new async deep-resolution tests + 39 existing); full arvel suite
      green; mypy --strict (1047 files) + pyright (0 errors) + ruff check + ruff format clean. Docs
      service-container.md states amake resolves async bindings at any depth and sync make still
      rejects them. AsyncSession scope + alias()/extend() deferred.
    timestamp: "2026-06-08T14:05:00Z"
  - work_item: "WI-arvel-018"
    stage: "research"
    event: "enter"
    result: "pass"
    note: >-
      Module 18 (config). C1 confirmed via repro: lookup() tried attribute access before dict
      subscript, so a missing dict key named like a dict builtin (get/items/keys/values/pop/...)
      resolved to the bound method instead of missing — breaking config(key, default) and Laravel
      array-is-key-lookup parity. Secret redaction, to_jsonable, source precedence verified correct.
    artifacts:
      - ".context/research/018-config.md"
      - "docs/pipeline/specs/WI-arvel-018-config-dict-key-shadowing.md"
    timestamp: "2026-06-08T14:10:00Z"
  - work_item: "WI-arvel-018"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      _lookup_registry.lookup(): traversal now branches on cursor type — dict segments are key-only
      (raise ConfigKeyError on miss, never fall through to attributes); modules/namespaces/objects
      use attribute access unchanged. New tests/config/test_wi_018_dict_key_shadowing.py (25 cases:
      11 dict builtins x config+lookup, plus real-key, default, and namespace-attr paths). No shims.
    artifacts:
      - "packages/arvel/src/arvel/config/_lookup_registry.py"
      - "packages/arvel/tests/config/test_wi_018_dict_key_shadowing.py"
      - "docs/site/docs/core-concepts/configuration.md"
    timestamp: "2026-06-08T14:18:00Z"
  - work_item: "WI-arvel-018"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Config + console suites 786 passed; full arvel suite 4364 passed, 0 failures; mypy --strict
      clean, pyright 0 errors, ruff check + ruff format clean. configuration.md states dict segments
      are key-only. URL-embedded-credential redaction and config() array-set form deferred.
    timestamp: "2026-06-08T14:22:00Z"
  - work_item: "WI-arvel-019"
    stage: "research"
    event: "enter"
    result: "pass"
    note: >-
      Module 19 (logging). C1 confirmed via repro: _redact used exact key matching, so common
      credential field names (access_token/refresh_token/client_secret/api_secret/db_password/
      proxy_authorization) leaked to logs despite the redaction layer. Inconsistent with
      config._is_secret_key (substring). Attribute precedence, with_context/channel, exc_info
      capture verified correct.
    artifacts:
      - ".context/research/019-logging.md"
      - "docs/pipeline/specs/WI-arvel-019-log-redaction-substring.md"
    timestamp: "2026-06-08T14:30:00Z"
  - work_item: "WI-arvel-019"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      otel_logger._redact now matches secret hints by substring (mirrors config._is_secret_key);
      a field is redacted when any configured hint is a substring of its lowercased name. Fail-closed,
      tunable via LOG_REDACT_FIELDS. New tests/observability/test_wi_019_log_redaction.py (8 cases via
      the public Log facade + FakeObservability). No shims.
    artifacts:
      - "packages/arvel/src/arvel/logging/otel_logger.py"
      - "packages/arvel/tests/observability/test_wi_019_log_redaction.py"
      - "docs/site/docs/features/logging.md"
    timestamp: "2026-06-08T14:38:00Z"
  - work_item: "WI-arvel-019"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Observability + context suites 146 passed; full arvel suite 4372 passed, 0 failures; mypy
      --strict clean, pyright 0 errors, ruff check + ruff format clean. logging.md documents
      substring redaction + LOG_REDACT_FIELDS. Nested-dict redaction and log-level default
      divergence deferred (documented).
    timestamp: "2026-06-08T14:42:00Z"
  - work_item: "WI-arvel-020"
    stage: "research"
    event: "enter"
    result: "pass"
    note: >-
      Module 20 (support: Str/Arr/Collection). C1 confirmed via repro: Collection.intersect/diff
      compared by id() not value, so value-equal dicts/models/runtime-strings never matched;
      inconsistent with only/except_ (which use ==) and Laravel. Str.limit rtrim, slug @->at, and
      Arr numeric-index traversal noted as minor parity, deferred.
    artifacts:
      - ".context/research/020-support.md"
      - "docs/pipeline/specs/WI-arvel-020-collection-value-equality.md"
    timestamp: "2026-06-08T14:50:00Z"
  - work_item: "WI-arvel-020"
    stage: "execution"
    event: "exit"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Collection.intersect/diff now match by value (item in other / not in other), consistent with
      only/except_ and Laravel; works for unhashable members. New
      tests/support/test_wi_020_collection_value_equality.py (7 cases). Existing object()-based
      identity tests still pass. No shims.
    artifacts:
      - "packages/arvel/src/arvel/support/collections.py"
      - "packages/arvel/tests/support/test_wi_020_collection_value_equality.py"
    timestamp: "2026-06-08T14:56:00Z"
  - work_item: "WI-arvel-020"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Support suite 245 passed (48 collection incl. 7 new); full arvel suite 4379 passed, 0 failures;
      mypy --strict clean, uv run pyright 0 errors, ruff check + ruff format clean. Docstrings state
      value-equality. No user-facing Collection-helper doc page exists; docstring is the source of truth.
    timestamp: "2026-06-08T15:00:00Z"
  - work_item: "WI-arvel-021"
    stage: "qa-post"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 21 (i18n). C1 confirmed via repro: select_plural_variant indexed positional variants by
      raw count, so "apple|apples" returned "apples" at count=1 and "apple" at count=0 — opposite of
      Laravel's getPluralIndex (English: singular only at count==1). Docs even claimed count=1 -> "1 apple".
    timestamp: "2026-06-08T15:10:00Z"
  - work_item: "WI-arvel-021"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Ported Laravel MessageSelector::getPluralIndex as a flat dispatch table of small per-family rule
      fns (zero/two-form/zero-or-one/Slavic/Czech/Irish/Lithuanian/Slovenian/Macedonian/Maltese/Latvian/
      Polish/Welsh/Romanian/Arabic), keyed by language subtag (pt from pt_BR). Threaded the active locale
      from Translator.choice into select_plural_variant. Bracket path unchanged. Updated test_choice
      (count-indexed -> Laravel contract); new test_wi_021_plural_rules.py.
    artifacts:
      - "packages/arvel/src/arvel/i18n/pluralisation.py"
      - "packages/arvel/src/arvel/i18n/translator.py"
      - "packages/arvel/tests/i18n/test_choice.py"
      - "packages/arvel/tests/i18n/test_wi_021_plural_rules.py"
      - "pyproject.toml"
    timestamp: "2026-06-08T15:16:00Z"
  - work_item: "WI-arvel-021"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      i18n suite 94 passed (incl. new plural-rule cases); full arvel suite 4409 passed, 0 failures;
      mypy --strict clean, pyright 0 errors, ruff check + ruff format clean. PLR2004/ARG001 per-file
      ignore added for the CLDR rule table (domain constants), matching existing repo precedent.
      localization.md pluralisation section updated.
    timestamp: "2026-06-08T15:20:00Z"
  - work_item: "WI-arvel-022"
    stage: "qa-post"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 22 (context). C1 confirmed against Laravel 13 docs (context7 /laravel/docs):
      Context::dehydrate captures both data AND hidden; the dehydrating example adds hidden 'locale'
      so it rides along to the job and is read via getHidden in hydrated. Arvel dropped hidden on
      dehydrate and merged-visible-only on hydrate, framing it as intentional ("must not leave the
      process") — a misread of Laravel's hidden semantics. Maintenance module reviewed: no defect.
    timestamp: "2026-06-08T15:35:00Z"
  - work_item: "WI-arvel-022"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      dehydrate() now returns {"data":..., "hidden":...}; hydrate(payload) replaces both stores
      (Laravel sets, not merges). Hidden still excluded from all()/get()/logs. Facade signatures +
      repository docstring corrected. Updated test_context_repository (excludes-hidden -> includes-hidden,
      added replace-semantics case); new test_wi_022_context_dehydrate_hidden.py (4 cases).
    artifacts:
      - "packages/arvel/src/arvel/context/repository.py"
      - "packages/arvel/src/arvel/context/facade.py"
      - "packages/arvel/tests/context/test_context_repository.py"
      - "packages/arvel/tests/context/test_wi_022_context_dehydrate_hidden.py"
    timestamp: "2026-06-08T15:41:00Z"
  - work_item: "WI-arvel-022"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      context suite 27 passed (incl. 4 new); full arvel suite 4414 passed, 0 failures; mypy --strict
      clean, uv run pyright 0 errors, ruff check + ruff format clean. No user-facing context doc page
      (facades.md mention is generic) — repository docstring is the source of truth.
    timestamp: "2026-06-08T15:45:00Z"
  - work_item: "WI-arvel-023"
    stage: "qa-post"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 23 (application kernel). C1 confirmed by inspection: Application.shutdown() drained
      services best-effort but providers fail-fast — first provider.shutdown() to raise re-raised
      immediately, stranding every provider after it in reverse order. DatabaseServiceProvider
      (engine.dispose) sits after Scheduler/Http/user/Console in that order, so any earlier failure
      leaks the connection pool on graceful shutdown. C2: raise preceded _booted=False, leaving the
      app marked booted. Boot stays correctly fail-fast (BootError); only teardown was wrong.
    timestamp: "2026-06-08T15:50:00Z"
  - work_item: "WI-arvel-023"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      shutdown() now drains every provider in reverse, logs each failure (provider.shutdown_failed),
      sets _booted=False unconditionally, then re-raises ShutdownError for the first failing provider
      (public API preserved). Mirrors the services loop above it. Added test_wi_023_shutdown_drains.py
      (2 cases: no-strand of later teardowns + booted cleared on failure); existing
      test_shutdown_error_wraps_provider_failure still green.
    artifacts:
      - "packages/arvel/src/arvel/application/application.py"
      - "packages/arvel/tests/application/test_wi_023_shutdown_drains.py"
    timestamp: "2026-06-08T15:56:00Z"
  - work_item: "WI-arvel-023"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      application + qa_post edge cases 95 passed (incl. 2 new); full arvel suite 4416 passed, 0
      failures; mypy --strict clean, uv run pyright 0 errors, ruff check + ruff format clean.
    timestamp: "2026-06-08T16:00:00Z"
  - work_item: "WI-arvel-023"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Doc follow-up: core-concepts/lifecycle.md Shutdown section now states teardown is best-effort
      (every provider.shutdown() runs even if an earlier one raises; first failure re-raised as
      ShutdownError after all have run; app always left un-booted). ShutdownError table row clarified.
    artifacts:
      - "docs/site/docs/core-concepts/lifecycle.md"
    timestamp: "2026-06-08T16:08:00Z"
  - work_item: "WI-arvel-024"
    stage: "qa-pre"
    event: "enter"
    result: "pass"
    note: >-
      arvel shell crashed at boot when the DB was unreachable: DatabaseServiceProvider.boot() ran an
      eager SELECT 1 ping → DatabaseConnectionError → BootError (after blocking on DNS = the "slow").
      Laravel Tinker survives a dead DB via lazy connections. Research 024 confirmed the ping is the
      only eager network probe on the boot path (DatabaseService.connect() is a no-op).
    timestamp: "2026-06-08T16:20:00Z"
  - work_item: "WI-arvel-024"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Added Application.boot(*, probe_connections=True) + probe_connections() accessor; DB provider
      runs the ping only when probing is on but wires engine/session-maker/DB facade unconditionally.
      ShellCommand.run_repl now boots with probe_connections=False, so the REPL opens lazily and
      queries connect on first use. Server/CLI keep eager fail-fast (default True). Tests: provider
      probe-on raises (BootError → DatabaseConnectionError), probe-off opens with bindings wired,
      lazy ORM query works on a healthy DB; shell run_repl boots without the probe.
    artifacts:
      - "packages/arvel/src/arvel/application/application.py"
      - "packages/arvel/src/arvel/providers/database_provider.py"
      - "packages/arvel/src/arvel/console/commands/shell.py"
      - "packages/arvel/tests/database/test_database_provider.py"
      - "packages/arvel/tests/console/test_shell_extended.py"
    timestamp: "2026-06-08T16:26:00Z"
  - work_item: "WI-arvel-024"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      console + database + application suites 1948 passed, 0 failures; mypy (1056 files) clean,
      pyright 0 errors/warnings, ruff check clean. Doc note added to docs/site/docs/cli/commands.md
      (shell/tinker connect lazily; serve/migrate keep eager fail-fast).
    timestamp: "2026-06-08T16:30:00Z"
  - work_item: "WI-arvel-025"
    stage: "qa-pre"
    event: "enter"
    result: "pass"
    note: >-
      Reported from the shell: Model.where("email", "x") and where("email", "ilike", "x") raised
      SQLAlchemy ArgumentError ("'email' should be explicitly declared as text('email')").
      QueryBuilder.where only handled expression/callable/kwargs forms — bare strings fell through
      to and_() and got coerced as raw SQL. or_where shared the gap via _or_terms. _apply_operator
      + _WHERE_ANY_OPS already existed (used by having/where_any) but were never wired into where.
    timestamp: "2026-06-08T16:45:00Z"
  - work_item: "WI-arvel-025"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Added _string_clause_predicate: when clauses[0] is a str, parse where(col, value) →
      col == value and where(col, op, value) → _apply_operator (unknown op → ValueError; lone
      string → TypeError). where and _or_terms consult it; expression/group/kwargs paths unchanged.
      Added test_wi_025_where_string_operator.py (8 cases: implicit eq, operator, comparison, AND
      chain, or_where, bad operator, lone string, expression-form regression).
    artifacts:
      - "packages/arvel/src/arvel/database/query.py"
      - "packages/arvel/tests/database/test_wi_025_where_string_operator.py"
    timestamp: "2026-06-08T16:52:00Z"
  - work_item: "WI-arvel-025"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Full database suite 1151 passed, 0 failures; mypy (1057 files) clean, pyright 0
      errors/warnings, ruff check clean. Updated docs/site/docs/orm/query-builder.md — replaced the
      stale "three-arg string form not supported" WARNING with the now-supported forms.
    timestamp: "2026-06-08T16:56:00Z"
  - work_item: "WI-arvel-026"
    stage: "qa-post"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 26 (arvel-search). C1 confirmed via repro: MeilisearchEngine.search built filters as
      f'{name} = "{value}"' for every value. where("price",100)->'price = "100"' (string compare vs
      numeric field, matches nothing), where("active",True)->'active = "True"' (Python-cased + quoted
      vs Meilisearch bare 'true'), where("name",'a"b')->'name = "a"b"' (unescaped quote breaks/rewrites
      the expression; filters come from request params). Laravel Scout emits numbers/booleans bare.
      Elasticsearch engine is correct (native types in a term query) — Meilisearch-only.
    timestamp: "2026-06-08T17:05:00Z"
  - work_item: "WI-arvel-026"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Added _filter_clause(value): None->IS NULL; bool->'= true'/'= false' (checked before int, a bool
      subclass); int/float->'= <value>' bare; else->'= "<escaped>"' with \\ and " escaped. search()
      now renders filters via it. Added 2 Meilisearch tests (typed values render bare; string value
      escaped). search suite 61 passed.
    artifacts:
      - "packages/arvel-search/src/arvel_search/engines/meilisearch.py"
      - "packages/arvel-search/tests/test_engines.py"
      - "docs/site/docs/packages/search.md"
    timestamp: "2026-06-08T17:11:00Z"
  - work_item: "WI-arvel-026"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      arvel-search suite 61 passed (2 new); mypy --strict clean, uv run pyright 0 errors, ruff check +
      ruff format clean. search.md notes where filters keep their type and strings are escaped.
    timestamp: "2026-06-08T17:15:00Z"
  - work_item: "WI-arvel-027"
    stage: "qa-post"
    event: "review"
    result: "pass"
    note: >-
      Module 27 (arvel-oauth). C1 confirmed via repro: MicrosoftProvider.get_user set
      email_verified=bool(data.get("email_verified", email is not None)) — defaults True whenever any
      email is present. Entra /oidc/userinfo omits the claim and its email/preferred_username isn't
      guaranteed verified (guest/MSA/federated), so every Microsoft login read as verified. The linker
      (_resolve_user/_create_user) only attaches to / adopts an existing user's email when verified, to
      stop unverified-claim takeover; OAuthUser.email_verified defaults False. Google/OIDC default False,
      Apple verifies id_token via JWKS — Microsoft was the outlier. GitHub /user.email (GitHub-constrained
      to verified public emails) and non-constant-time state compare reviewed, no change.
    timestamp: "2026-06-08T18:40:00Z"
  - work_item: "WI-arvel-027"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      MicrosoftProvider now defaults email_verified to False when the claim is absent (matches Google/OIDC
      and the DTO/linker contract); explicit email_verified:true still honoured. Added 2 tests
      (absent-claim -> unverified; explicit-claim -> honoured). arvel-oauth suite 38 passed.
    artifacts:
      - "packages/arvel-oauth/src/arvel_oauth/providers/microsoft.py"
      - "packages/arvel-oauth/tests/test_providers.py"
      - "docs/site/docs/packages/oauth.md"
    timestamp: "2026-06-08T18:46:00Z"
  - work_item: "WI-arvel-027"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      arvel-oauth suite 38 passed (2 new); mypy (-p arvel_oauth, 23 files) clean, uv run pyright 0 errors,
      ruff check + ruff format clean. oauth.md documents the linker email-verification contract and
      Microsoft's conservative default.
    timestamp: "2026-06-08T18:50:00Z"
  - work_item: "WI-arvel-028"
    stage: "qa-post"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 28 (arvel-permission). C1 confirmed via repro: MorphToMany writes the pivot model_type as
      get_morph_alias(owner) (override -> morph_map alias -> short class name), but the four class helpers
      query_with/without_role and query_with/without_permission filtered model_type == cls.__name__. Under
      a registered morph_map/__morph_class__ the tokens diverge: query_with_* returned [] and query_without_*
      returned everyone (incl. actual holders, ~pk.in_(empty) excludes nobody) — A01-adjacent for bulk admin
      tooling. Repro: morph_map({"m_quser": _MQUser}) -> stored 'm_quser', has_role True, query_with_role [].
      Instance checks (has_role/has_permission_to) use the accessor and were always correct. Swept arvel-audit
      (redaction applied on every create/update/delete path) and arvel-image (filename sanitization,
      content-based MIME, decompression-bomb ceiling) — clean.
    timestamp: "2026-06-08T19:00:00Z"
  - work_item: "WI-arvel-028"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Replaced cls.__name__ with get_morph_alias(cls) in all four query helpers (matches the pivot writer).
      Default no-morph-map case unchanged (alias falls back to short class name). Added
      test_052_query_morph_alias.py covering query_with/without_role and query_with/without_permission under a
      registered morph map. arvel-permission suite 77 passed.
    artifacts:
      - "packages/arvel-permission/src/arvel_permission/traits.py"
      - "packages/arvel-permission/tests/test_052_query_morph_alias.py"
      - "docs/site/docs/packages/permission.md"
    timestamp: "2026-06-08T19:06:00Z"
  - work_item: "WI-arvel-028"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      arvel-permission suite 77 passed (2 new); mypy (-p arvel_permission, 12 files) clean, uv run pyright 0
      errors, ruff check + ruff format clean. permission.md documents the query helpers and that they honor
      morph_map/__morph_class__.
    timestamp: "2026-06-08T19:10:00Z"
  - work_item: "WI-arvel-030"
    stage: "qa-post"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 30 (observability + bootstrap health). C1 (A01): add_metrics_route and add_health_route resolved
      the client IP from the first X-Forwarded-For value unconditionally, so a spoofed header (e.g.
      X-Forwarded-For: 127.0.0.1 for metrics' loopback default, or an allowlisted IP for HEALTH_ALLOWED_CIDRS)
      bypassed the CIDR guard and exposed internal telemetry / per-service health+exception detail. reverb
      already had the correct trusted-proxy model (resolve_remote_ip). The existing test
      test_metrics_route_allows_forwarded_clients_and_bad_cidrs asserted the insecure 200. throttle_login keys
      on the real peer (scope["client"]) — already safe.
    timestamp: "2026-06-08T20:20:00Z"
  - work_item: "WI-arvel-030"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Added shared arvel/observability/forwarded.py (resolve_client_ip + ip_in_cidrs) mirroring reverb: XFF
      honored only when the TCP peer is a configured trusted proxy (right-most-untrusted hop), else the peer is
      the client. Both routes use it; new ObservabilityConfig.trusted_proxies (OBSERVABILITY_TRUSTED_PROXIES,
      default []), trusted_proxies param on both registrars, threaded into _add_health_route. Rewrote the
      insecure metrics test, added health spoof regression + resolver unit tests.
    artifacts:
      - "packages/arvel/src/arvel/observability/forwarded.py"
      - "packages/arvel/src/arvel/observability/metrics_route.py"
      - "packages/arvel/src/arvel/observability/config.py"
      - "packages/arvel/src/arvel/health.py"
      - "packages/arvel/src/arvel/application/application.py"
      - "packages/arvel/tests/observability/test_forwarded_ip.py"
      - "packages/arvel/tests/observability/test_metrics_route.py"
      - "packages/arvel/tests/services/test_health_endpoint.py"
    timestamp: "2026-06-08T20:28:00Z"
  - work_item: "WI-arvel-030"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      observability + health suites green (1 pre-existing cwd-dependent failure in test_wi_030_config skeleton,
      unrelated — fails on clean tree too); mypy + uv run pyright 0 errors, ruff check + format clean. Spoofed
      XFF can no longer bypass either guard; trusted-proxy deployments still resolve the real client.
    timestamp: "2026-06-08T20:32:00Z"
  - work_item: "WI-arvel-029"
    stage: "qa-post"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 29 (auth / password-reset storage). C1: create_password_resets_table declared column "token"
      but PasswordReset model + PasswordService use "token_hash" — apps running the published migration hit
      "no such column: token_hash" on first forgot/reset; unit tests passed only because they build tables
      from model metadata. C2: auth:clear-resets inspected/deleted from "password_reset_tokens" but the real
      table is "password_resets", so it always errored. Sibling tables (PAT token, refresh_tokens token_hash)
      verified consistent — only password_resets drifted. Repro: model cols [email, token_hash, created_at]
      vs migration cols [email, token, created_at].
    timestamp: "2026-06-08T19:20:00Z"
  - work_item: "WI-arvel-029"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Converged on the canonical name table=password_resets, column=token_hash. Migration token -> token_hash
      (+ docstring); auth:clear-resets password_reset_tokens -> password_resets (inspect/delete/error/help) and
      regenerated _command_meta.py. Added a drift guard in test_framework_migrations.py asserting the migration
      columns cover the ORM model columns for password_resets/personal_access_tokens/refresh_tokens. Fixed
      test_command_helpers_more.py to build the correct table/column.
    artifacts:
      - "packages/arvel/src/arvel/auth/migrations/create_password_resets_table.py"
      - "packages/arvel/src/arvel/console/commands/auth_clear_resets.py"
      - "packages/arvel/src/arvel/console/_command_meta.py"
      - "packages/arvel/tests/database/test_framework_migrations.py"
      - "packages/arvel/tests/console/test_command_helpers_more.py"
    timestamp: "2026-06-08T19:26:00Z"
  - work_item: "WI-arvel-029"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      auth + console + migration suites 984 passed; mypy + uv run pyright 0 errors, ruff check + format clean
      on changed files. Drift guard now prevents the migration/model column mismatch from recurring.
    timestamp: "2026-06-08T19:30:00Z"
  - work_item: "WI-arvel-031"
    stage: "qa-post"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 31 (URL signing + token-comparison guards). C1 (A10): hmac/secrets.compare_digest raises TypeError
      on non-ASCII str, so six guards comparing an attacker-controlled str token crash into a 500 instead of the
      fail-closed reject — has_valid_signature (?signature=), VerifyCsrf (X-CSRF-TOKEN), csrf_double_submit (cookie/
      header), maintenance bypass (cookie/?bypass=), url_signer.verify (?token=), reverb verify_channel_auth (auth).
      Repro: URL.has_valid_signature(req("signature=caf%C3%A9")) -> TypeError; ASCII bogus sig -> False. Cookie store
      and token abilities compare bytes — safe; EmailVerificationService uses itsdangerous — safe.
    timestamp: "2026-06-08T21:00:00Z"
  - work_item: "WI-arvel-031"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Added arvel/support/secure_compare.py::constant_time_equals(a, b) comparing UTF-8 bytes via
      hmac.compare_digest — timing-safe, returns False on non-ASCII instead of raising (matches PHP hash_equals).
      Routed all six sites through it; removed now-unused import secrets from the CSRF/maintenance modules. Added a
      secure_compare unit suite + signed-URL non-ASCII 403 regression; updated source-introspection guards in
      reverb/csrf/storage/broadcasting/security suites to assert constant_time_equals.
    artifacts:
      - "packages/arvel/src/arvel/support/secure_compare.py"
      - "packages/arvel/src/arvel/routing.py"
      - "packages/arvel/src/arvel/http/_middleware_core.py"
      - "packages/arvel/src/arvel/auth/middleware/csrf_double_submit.py"
      - "packages/arvel/src/arvel/maintenance/middleware.py"
      - "packages/arvel/src/arvel/storage/url_signer.py"
      - "packages/arvel/src/arvel/reverb/auth.py"
      - "packages/arvel/tests/support/test_secure_compare.py"
      - "packages/arvel/tests/routing/test_wi053_routing_polish.py"
      - "packages/arvel/tests/broadcasting/test_security.py"
      - "packages/arvel/tests/storage/test_temporary_urls.py"
      - "packages/arvel/tests/reverb/test_auth.py"
      - "packages/arvel/tests/http/middleware/test_csrf.py"
      - "packages/arvel/tests/security/test_http_safety.py"
    timestamp: "2026-06-08T21:08:00Z"
  - work_item: "WI-arvel-031"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean on changed files; uv run mypy 0 issues (1062 files); uv run pyright 0 errors.
      Affected suites green (support/routing/csrf/maintenance/storage/reverb/broadcasting/security). Two pre-existing
      suite failures unrelated and out of scope: test_nosec_annotations (bare # nosec codes in untouched
      console/_venv.py) and test_wi_030_config (cwd-dependent skeleton path, flagged in WI-arvel-030).
    timestamp: "2026-06-08T21:12:00Z"
  - work_item: "WI-arvel-032"
    stage: "qa-post"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 32 (ORM serialization boundary). C1 (A01/A09): Model.to_dict() honours __hidden__/__visible__, but an
      Arvel Model is a MappedAsDataclass — FastAPI's jsonable_encoder serialises every column, so a handler's bare
      `return user` leaks hidden columns (password_hash, tokens). Laravel's `return $user;` applies $hidden. Framework
      + ecommerce kit unaffected (all return Pydantic/JsonResource/response_model). Mass assignment guard and
      casts/mutators reviewed — sound; insecure-by-default mass assignment is a deliberate design stance, not a bug.
    timestamp: "2026-06-08T21:40:00Z"
  - work_item: "WI-arvel-032"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Added routing._coerce_models_in_result + _wrap_response_normalizer, wired into Router.register_with_app for
      routes without an explicit response_model. Raw Model -> to_dict(); list of models -> per-element to_dict();
      everything else (dict/Pydantic/Response/primitives) passes through. to_dict reads columns only (no async relation
      load); handler __signature__ preserved so FastAPI DI is unchanged. Added test_wi054_hidden_field_leak covering
      raw model + list returns honouring hidden, and dict/Pydantic passthrough.
    artifacts:
      - "packages/arvel/src/arvel/routing.py"
      - "packages/arvel/tests/routing/test_wi054_hidden_field_leak.py"
    timestamp: "2026-06-08T21:48:00Z"
  - work_item: "WI-arvel-032"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean on changed files; uv run mypy 0 issues (1064 files); uv run pyright 0 errors on
      routing.py. Full suite 4463 passed / 1 failed — the one failure is the pre-existing test_nosec_annotations (bare
      # nosec codes in untouched console/_venv.py), unrelated and out of scope.
    timestamp: "2026-06-08T21:54:00Z"
  - work_item: "WI-arvel-033"
    stage: "qa-post"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 33 (logging). Follow-up to WI-019's deferred "nested-dict redaction". C1 (A09): otel_logger._redact
      scrubbed only top-level attribute keys, so a secret nested under a non-secret key leaked —
      Log.info("login", payload={"password": ...}) emitted the password verbatim; same for lists of dicts. The function
      comment claimed it "Mirrors config._is_secret_key", whose partner _strip_secrets is recursive, so code and intent
      disagreed.
    timestamp: "2026-06-08T22:10:00Z"
  - work_item: "WI-arvel-033"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Made _redact recurse through dicts and lists to any depth (mirrors config._strip_secrets; masks instead of
      dropping). Substring matching and the fail-closed LOG_REDACT_FIELDS default set unchanged, now applied at every
      depth. Corrected the misleading comment. Added test_wi_033_log_redaction_nested (6 cases): nested dict, deep
      nesting, list of dicts, non-secret passthrough, top-level still redacted, custom redact set on nested keys.
    artifacts:
      - "packages/arvel/src/arvel/logging/otel_logger.py"
      - "packages/arvel/tests/observability/test_wi_033_log_redaction_nested.py"
    timestamp: "2026-06-08T22:16:00Z"
  - work_item: "WI-arvel-033"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean on changed files; uv run mypy 0 issues (1064 files); uv run pyright 0 errors, 0 warnings.
      Observability + log-binding suites green (141 passed); the lone failure is the pre-existing cwd-dependent
      test_wi_030_config (flagged since WI-arvel-030), unrelated and out of scope.
    timestamp: "2026-06-08T22:22:00Z"
  - work_item: "WI-arvel-034"
    stage: "qa-post"
    event: "enter"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Module 34 (cache rate limiter). C1 (correctness/parity): Cache.rate_limiter().attempt reset the cache TTL on every
      hit (put ttl=decay), so the window slid forward with traffic — contradicting its own docstring ("resets after decay
      seconds from the first hit") and Laravel's fixed-window RateLimiter (Cache::add timer + atomic increment). A capped
      key stayed capped relative to the last allowed hit; remaining() never recovered under steady traffic. The Throttle
      middleware store (http/ratelimit, Redis INCR) was already correct — this is the cache-facade limiter only.
    timestamp: "2026-06-08T22:50:00Z"
  - work_item: "WI-arvel-034"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Store the window as {"hits", "reset_at"} and preserve the original expiry — increments use only the remaining TTL,
      never a fresh decay. Typed _window() helper (returns (hits, reset_at) for a live window, else None) shared by
      attempt/remaining; dict value round-trips through every store. Added 2 clock-driven tests (fixed-window non-sliding;
      remaining resets after the window) with no real-time sleeps. Cross-process atomicity left as a documented limitation
      (CacheStore has no atomic increment/add; same stance as lock()/remember()) — Throttle/Redis INCR is the race-free path.
    artifacts:
      - "packages/arvel/src/arvel/cache/rate_limiter.py"
      - "packages/arvel/tests/cache/test_rate_limiter.py"
      - "docs/site/docs/features/cache.md"
    timestamp: "2026-06-08T22:56:00Z"
  - work_item: "WI-arvel-034"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1065 files); uv run pyright 0 errors, 0 warnings. Cache + throttle +
      auth-security + auth-manager suites green (190 passed; warnings are the pre-existing process-local lock notices).
    timestamp: "2026-06-08T23:02:00Z"
  - work_item: "WI-arvel-035"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Security follow-up to WI-032. _coerce_models_in_result only handled a top-level Model or list; a model nested in a
      returned dict/tuple (e.g. {"user": user}) bypassed it and leaked __hidden__ columns. Made the normalizer recurse:
      a local coerce() converts Model->to_dict() and rebuilds dict/list/tuple, leaving Pydantic/Response/primitives
      untouched. to_dict() reads columns only, so no recursion blow-up or relation loads. Added a nested-leak regression
      test to the WI-054 routing suite.
    artifacts:
      - "packages/arvel/src/arvel/routing.py"
      - "packages/arvel/tests/routing/test_wi054_hidden_field_leak.py"
      - "docs/site/docs/orm/models.md"
    timestamp: "2026-06-08T23:10:00Z"
  - work_item: "WI-arvel-035"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1065 files); uv run pyright 0 errors, 0 warnings; WI-054 routing
      suite 5 passed (top-level model/list/non-model/pydantic + new nested-dict case).
    timestamp: "2026-06-08T23:12:00Z"
  - work_item: "WI-arvel-036"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Gate.allows() resolved a policy straight to its ability method, skipping the policy before() filter Laravel runs
      first (True grants all, False denies all, None falls through) — an A01 gap: a banned-user global deny or admin
      global grant in a policy was silently dropped. Policy.check() had the same omission. Added the before() short-circuit
      to both (sync/async, getattr+callable so non-method attrs aren't invoked); order is gate-before -> policy-before ->
      ability method. Confirmed semantics against /laravel/docs 13.x authorization.md. 6 new gate tests.
    artifacts:
      - "packages/arvel/src/arvel/auth/gate.py"
      - "packages/arvel/src/arvel/auth/policy.py"
      - "packages/arvel/tests/auth/test_gate.py"
      - "docs/site/docs/features/authorization.md"
    timestamp: "2026-06-08T23:22:00Z"
  - work_item: "WI-arvel-036"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1065 files); uv run pyright 0 errors, 0 warnings; gate suite
      14 passed (8 existing + 6 new policy-before cases).
    timestamp: "2026-06-08T23:24:00Z"
  - work_item: "WI-arvel-037"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Migrations audit. Migrator core (per-migration tx, batch/rollback/reset, fresh drop order, sync/async bridge,
      non-destructive discovery) reviewed sound — no change. Defect: migrate pre-flights with check_database_connection
      and exits 2 on DatabaseUnavailableError, but migrate:fresh/migrate:refresh skipped it and their except clauses
      didn't catch it, so a down DB dumped a raw traceback and exited 1 (A10). Added the pre-flight check + exit-2 handler
      to both destructive commands. 2 new parametrized tests (monkeypatched health check -> exit 2 + friendly message).
    artifacts:
      - "packages/arvel/src/arvel/console/commands/migrate_fresh.py"
      - "packages/arvel/src/arvel/console/commands/migrate_refresh.py"
      - "packages/arvel/tests/console/test_migrate_reset_family.py"
    timestamp: "2026-06-08T23:36:00Z"
  - work_item: "WI-arvel-037"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1065 files); uv run pyright 0 errors, 0 warnings; reset-family
      suite 11 passed (9 existing + 2 new DB-unavailable cases).
    timestamp: "2026-06-08T23:38:00Z"
  - work_item: "WI-arvel-038"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Queue reliability audit. Worker cancellation/retry/DLQ/chains/poison-pill reviewed sound. Defect (F2): the database
      driver deleted the row on pop, so a worker killed mid-handle (OOM/SIGKILL) lost the job — at-most-once with a loss
      window. Adopted Laravel's reserve-then-ack: pop stamps reserved_at + returns row id on envelope.receipt; the worker
      calls delete_reserved on completion; pop reclaims reservations older than retry_after (default 90s, env
      QUEUE_DATABASE_RETRY_AFTER); malformed/unknown rows are deleted outright. delete_reserved is duck-typed so
      redis/taskiq/sync are untouched. At-least-once now — documented idempotency requirement in the queue guide.
    artifacts:
      - "packages/arvel/src/arvel/queue/config.py"
      - "packages/arvel/src/arvel/queue/envelope.py"
      - "packages/arvel/src/arvel/queue/drivers/database.py"
      - "packages/arvel/src/arvel/queue/worker.py"
      - "packages/arvel/src/arvel/queue/migrations/create_jobs_table.py"
      - "packages/arvel/tests/test_queue/drivers/test_database.py"
      - "packages/arvel/tests/test_queue/drivers/test_database_connection_sql_integration.py"
      - "docs/site/docs/features/queues.md"
    timestamp: "2026-06-08T23:52:00Z"
  - work_item: "WI-arvel-038"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1065 files); uv run pyright 0 errors, 0 warnings; full queue suite
      233 passed (incl. postgres/mysql integration + 2 new reserve/redeliver cases).
    timestamp: "2026-06-08T23:54:00Z"
  - work_item: "WI-arvel-039"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Events dispatcher audit. Inline dispatch (DI + catch/continue), broadcast routing, and ListenerJob allowlist
      deserialization (ListenerRegistry/EventRegistry, A08-safe) reviewed sound. Defect (WI-009 deferred): _dispatch_queued
      wrapped Bus lookup + create + dispatch in one try/except: pass then ran the listener inline, so a configured-but-down
      broker swallowed the failure (no log) and ran a ShouldQueue listener synchronously in the publish path (blocking +
      double-run risk). Split: Bus.manager is None -> log + inline (dev fallback, unchanged); queue configured -> enqueue,
      on failure logger.exception(queued_listener_enqueue_failed) and return (never inline). Catch stays per-listener.
    artifacts:
      - "packages/arvel/src/arvel/events/dispatcher.py"
      - "packages/arvel/tests/test_events/test_dispatcher.py"
      - "docs/site/docs/features/events.md"
    timestamp: "2026-06-09T00:12:00Z"
  - work_item: "WI-arvel-039"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1065 files); uv run pyright 0 errors, 0 warnings; events suite
      32 passed (30 existing + 2 new enqueue/inline cases).
    timestamp: "2026-06-09T00:14:00Z"
  - work_item: "WI-arvel-040"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      HTTP middleware stack audit. Cors, security headers, method spoof, throttle (fixed-window), database transaction
      (commit 2xx/3xx, rollback on error, after-commit on success), signed, both CSRF middlewares, forwarded-IP guards
      reviewed sound (prior WIs). Defect: VerifiedMiddleware raised UnauthenticatedException (401) for BOTH no-user and
      logged-in-but-unverified; the latter is a 403 authorization failure (re-auth won't help — must verify email), per
      Laravel EnsureEmailIsVerified. Split: None -> 401; unverified -> AuthorizationException (403). http_provider
      translators already map auth.AuthorizationException -> HTTP 403.
    artifacts:
      - "packages/arvel/src/arvel/auth/middleware/verified.py"
      - "packages/arvel/tests/auth/test_middleware.py"
      - "docs/site/docs/features/authentication.md"
    timestamp: "2026-06-09T00:32:00Z"
  - work_item: "WI-arvel-040"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1065 files); uv run pyright 0 errors, 0 warnings; auth suites
      254 passed (incl. 1 tightened + 1 new verified-middleware case).
    timestamp: "2026-06-09T00:34:00Z"
  - work_item: "WI-arvel-041"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      ORM pagination audit. paginate (offset+COUNT on ORDER-BY-stripped subquery, page>=1), simple_paginate (per_page+1,
      has_more), cursor_paginate (keyset: flips directions for backward then reverses page, resets pre-existing ORDER BY,
      applies global scopes) reviewed sound and Laravel-aligned. Defect: a malformed ?cursor= raises InvalidCursorError
      (ORMError) from _decode_cursor with no HTTP translator -> 500 INTERNAL_ERROR and risked leaking the base64/JSON
      decode reason. A cursor is opaque client input -> 400. Fix: http_provider.default_translators maps
      InvalidCursorError -> BadRequestException("Invalid pagination cursor.") with a fixed message (no leak).
    artifacts:
      - "packages/arvel/src/arvel/providers/http_provider.py"
      - "packages/arvel/tests/http/test_wi061_model_not_found_404.py"
      - "docs/site/docs/orm/query-builder.md"
    timestamp: "2026-06-09T00:48:00Z"
  - work_item: "WI-arvel-041"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1065 files); uv run pyright 0 errors, 0 warnings; pagination suite
      82 passed; HTTP-translator file 11 passed (incl. 2 new cursor cases).
    timestamp: "2026-06-09T00:50:00Z"
  - work_item: "WI-arvel-042"
    stage: "qa-post"
    event: "fix"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      DB transactions/connections audit. DB.transaction (savepoint reentrancy, outermost owns the after-commit queue,
      fires after closing the session + resetting the active-session contextvar), transactional (deadlock/serialization
      retry only), imperative begin/commit/rollback stack, autocommit (independent AUTOCOMMIT connection), pretend
      (rollback), raw bound-param SQL reviewed sound. Defect: DatabaseTransaction middleware fired after-commit callbacks
      in the else branch INSIDE the async-with session block — after COMMIT but before reset_active_session — so a
      callback's ORM write opened a fresh implicit txn on the about-to-close session and was silently discarded (A10),
      diverging from DB.transaction(). Fix: track committed, fire callbacks after the session is closed+unbound; rollback
      paths (4xx/5xx/exception) skip them.
    artifacts:
      - "packages/arvel/src/arvel/http/middleware/database_transaction.py"
      - "packages/arvel/tests/http/test_database_transaction_middleware.py"
    timestamp: "2026-06-09T01:10:00Z"
  - work_item: "WI-arvel-042"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1065 files); uv run pyright 0 errors, 0 warnings; txn middleware +
      db edge/imperative 42 passed (incl. 2 new); full http + database suites 1348 passed.
    timestamp: "2026-06-09T01:12:00Z"
  - work_item: "WI-arvel-043"
    stage: "planning"
    event: "triage"
    result: "pass"
    note: >-
      Feature-gap bucket-3 triage. Verified the CHANGELOG Unreleased "remaining gaps" against the codebase: needs-based
      CLI bootstrap (CliSubsystem requires) and openapi:export --output FILE are already implemented (stale entries,
      moved to landed); the "~25 missing rules" line over-counted (32 already shipped). Ranked the 6 genuine remaining
      gaps by impact/effort/risk: (1) Http:: facade + fake, (2) response()/redirect(), (3) route:cache/clear, (4) more
      validation rules [recommended first — low risk], (5) CSRF consolidation + TrustProxies [high risk, security path,
      own gated WI], (6) STORAGE_LOCAL_SERVE. CHANGELOG de-staled in place (dropped nonexistent ROADMAP.md reference).
    artifacts:
      - "CHANGELOG.md"
      - "docs/backlog/043-epic-feature-gap-bucket3-triage.md"
    timestamp: "2026-06-09T01:30:00Z"
  - work_item: "WI-arvel-044"
    stage: "execution"
    event: "implement"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Validation parity round 2 (WI-043 gap #4, recommended first — additive, low risk). Existing rules layer
      reviewed (sometimes/Rule.sometimes conditional already shipped). Added: bail (validator stops a field at its
      first failure; the bail token never errors), conditional presence (required_if, required_unless, required_with,
      required_with_all, required_without, required_without_all — fail only when the trigger holds and the field is
      empty), date rules (date, date_format:FMT, before/after/before_or_equal/after_or_equal with literal | field-ref |
      today/now bounds; naive values normalized to UTC so comparisons are consistent and tz-safe), register_rule() to
      add custom handlers, and Rule builders (in_, not_in, exists, unique, required_if, required_unless) that emit rule
      strings. Wildcard/nested (items.*.id) deferred — it changes the validator's field-iteration model (own WI).
    artifacts:
      - "packages/arvel/src/arvel/validation/rules.py"
      - "packages/arvel/src/arvel/validation/validator.py"
      - "packages/arvel/src/arvel/validation/rule.py"
      - "packages/arvel/src/arvel/validation/__init__.py"
      - "packages/arvel/tests/validation/test_wi044_validation_parity.py"
      - "docs/site/docs/the-basics/validation.md"
      - "CHANGELOG.md"
    timestamp: "2026-06-09T01:50:00Z"
  - work_item: "WI-arvel-044"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1066 files); uv run pyright 0 errors, 0 warnings; validation
      suite 113 passed (incl. 27 new across bail, conditional presence, dates, custom registration, Rule builders);
      mkdocs build --strict clean.
    timestamp: "2026-06-09T01:52:00Z"
  - work_item: "WI-arvel-045"
    stage: "execution"
    event: "implement"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      Nested/wildcard validation — the last validation-parity item (WI-043 #4 remainder). Added resolve_targets() to
      the validator: a (possibly dotted/wildcard) field key expands to concrete (path, value, present) targets.
      Non-wildcard paths (address.city) always yield one target even when missing so required/present fire; `*`
      only iterates entries that exist (list indices or dict keys), so a missing collection yields nothing — Laravel
      parity. Explicit indices (items.0.id) and negative indices supported. The validate loop now runs each target's
      rules via _run_target (bail honored per target); errors key by the concrete path; nested targets get a
      path-scoped data view (_scoped_data) so present/filled/prohibited are path-aware; message overrides try the
      concrete path then the wildcard form, attribute labels likewise. Flat fields are unchanged and zero-copy
      (backward compatible). object->Mapping/list narrowing cast to concrete element types (cast, not ignore).
    artifacts:
      - "packages/arvel/src/arvel/validation/validator.py"
      - "packages/arvel/tests/validation/test_wi045_nested_wildcard.py"
      - "docs/site/docs/the-basics/validation.md"
      - "CHANGELOG.md"
    timestamp: "2026-06-09T02:05:00Z"
  - work_item: "WI-arvel-045"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1067 files); uv run pyright 0 errors, 0 warnings; validation
      suite 132 passed (incl. 19 new: resolve_targets unit cases, wildcard/nested/explicit-index validation,
      path-aware presence, wildcard+concrete message overrides, backward-compat).
    timestamp: "2026-06-09T02:07:00Z"
  - work_item: "WI-arvel-046"
    stage: "execution"
    event: "implement"
    result: "pass"
    gate: "auto-pass-eligible"
    note: >-
      response()/redirect() helpers (WI-043 feature gap). New arvel/http/responses.py: a ResponseFactory exposed via
      response() with json/text/make/no_content builders; redirect(to, status, headers) returning a Redirect
      (RedirectResponse subclass); to_route(name, **params) that resolves a named route via routing.route; back(request,
      fallback) reading the Referer header; and Redirect.with_(request, **values) which flashes into
      request.state.session (chainable, no-op when session middleware isn't active). Re-exported from arvel.http.
      routing imported lazily inside to_route to avoid an import cycle.
    artifacts:
      - "packages/arvel/src/arvel/http/responses.py"
      - "packages/arvel/src/arvel/http/__init__.py"
      - "packages/arvel/tests/http/test_wi046_response_redirect.py"
      - "docs/site/docs/the-basics/controllers.md"
      - "CHANGELOG.md"
    timestamp: "2026-06-09T02:20:00Z"
  - work_item: "WI-arvel-046"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff check + format clean; uv run mypy 0 issues (1069 files); uv run pyright 0 errors, 0 warnings; http suite
      209 passed (incl. 12 new: response builders, redirect status/location, with_ session flash + no-session no-op,
      back via Referer/fallback, to_route named-route URL); mkdocs build --strict clean.
    timestamp: "2026-06-09T02:22:00Z"
  - work_item: "WI-arvel-049"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      Verification-only — STORAGE_LOCAL_SERVE already implemented + committed in a prior session
      (providers/storage_provider.py, config/storage_config.py). Ran tests/storage/test_serve_route.py: 10 passed
      (serve route, traversal protection, signed/temporary URLs, disabled/absolute-url skip). CHANGELOG triage entry
      moved from 'remaining gaps' to 'landed'. No code change.
    timestamp: "2026-06-09T02:26:00Z"
  - work_item: "WI-arvel-048"
    stage: "execution"
    event: "decision"
    result: "wont-do"
    note: >-
      route:cache / route:clear resolved as N/A for Python. Traced the routing model: handlers and event listeners are
      live callables stored on RouteSpec (not Laravel string actions), so a route/event cache can't be both correct
      and useful — pickling callables is a code-exec risk and breaks across changes; storing module:qualname can't
      round-trip closures, decorated handlers, or dynamically-mounted controllers; and you must import the modules
      anyway (already .pyc-cached) to get the callables. Updated optimize to report route:cache/event:cache as 'n/a on
      Python' (was 'pending serializer'); removed the gap from the CHANGELOG roadmap and added a 'deliberately not
      implemented' note. No route:clear (no artifact to clear). Decision doc: .context/research/048-route-cache-decision.md.
    artifacts:
      - "packages/arvel/src/arvel/console/commands/optimize.py"
      - "CHANGELOG.md"
    timestamp: "2026-06-09T02:32:00Z"
  - work_item: "WI-arvel-048"
    stage: "qa-post"
    event: "exit"
    result: "pass"
    note: >-
      ruff clean on optimize.py; tests/console/test_optimize.py 5 passed (route:cache/event:cache substrings still
      present in output). Documentation-and-decision change; no behavioral regression.
    timestamp: "2026-06-09T02:33:00Z"
